# Windows-MCP 项目术语

本文维护项目内已确认的概念及边界，不替代完整需求，也不表示相关优化已经实施或通过验收。

## 交付基线

**定义：** 同一会话内，Windows-MCP 最近一次成功返回给 AI 的有效状态，用作下一次状态变化比较的候选基线。比较前仍须确认它与当前观察的范围和对象可比。

**保留范围：** 只维护最近一次交付基线，不维护多版本历史链，不把历史保存、传回或拼接交给 AI。正常比较所需的当前工作状态不属于历史回溯服务。此定义不决定具体保留秒数、磁盘持久化或跨重启恢复。

**边界：** WaitFor 内部轮询的中间状态、失败采集和未交付状态不替换交付基线。采集完成或响应生成不等同于成功交付；成功交付的工程判据尚待明确，不预设已经存在客户端接收确认机制，也不据此增加 AI 确认调用。不同会话不得混用基线。

**不可比情形：** 首次没有基线、观察范围不可比或窗口对象失效时，不强行计算差异；仍交付本次实际取得的完整当前状态，并明确说明没有可比基线或不可比原因。未观察区域、采集缺失或截断造成的缺项不等于元素已删除；无法比较不等于无变化。

**排除用语：** 此处“上次”不是最近一次内部采集，也不是修改前后 token 测试中的“原版输出”。正常使用不要求先运行原版。变化表示两个可比状态之间的净差异，不承诺恢复其间全部瞬态事件。

来源：[Windows-MCP 使用过程 Token 消耗优化需求草稿](PLAN/2026.09/2026.09.15/2026.09.15-01-需求提炼-Windows-MCP使用过程Token消耗优化.md)，C-024、C-025；对应 Q-023（含同题澄清 Q-023-A）、Q-024。

## 变化摘要

**定义：** 工具内部以可比的交付基线和当前状态进行确定性比较后，生成的高层变化描述，例如窗口变化、元素新增或删除、文本变化。描述须清楚对应受影响对象与变化类别；这些例子不是允许遗漏重要变化的封闭清单。

**与完整状态的关系：** 变化摘要随完整当前状态在同一次调用中返回，只是补充，不替代完整状态。默认不逐字段重复全部旧值、新值和坐标，也不采用只给节点 ID 或路径索引、让 AI 自行解释变化的方式。

**边界：** 不为生成摘要再调用模型，不让 AI 另行调用 diff 或重新比较历史，不将必要信息转为额外补查。完整当前状态中的层级、名称、类型、逐词坐标、动作、值及相关状态仍按已确认要求保留；该定义不授权自动截图、默认截短控件文本或降低采集上限。

**可靠性边界：** 变化摘要遵守交付基线的会话隔离与可比性要求；不能把未知、未观察或截断误报为无变化或删除。摘要无法清楚表达重要变化、导致额外比较或补查时，需要重新裁决表达方式，不能用含混提示替代必要信息。具体字段与格式尚未确定。

来源：[Windows-MCP 使用过程 Token 消耗优化需求草稿](PLAN/2026.09/2026.09.15/2026.09.15-01-需求提炼-Windows-MCP使用过程Token消耗优化.md)，C-026；对应 Q-025，并关联 C-004、C-016、C-023 至 C-025。
