# 需求提炼：Windows-MCP 使用过程 Token 消耗优化

> Grill-Me 工作草稿, 尚未形成正式需求版本

创建日期：2026.09.15

## 会谈输入

### 已确定目标与写入位置

- 访谈目标完整原文：`减少 Windows-MCP 在使用过程中的 token 消耗`
- 业务项目：`xrkk/Windows-MCP`。
- 固定文档写入目录：`https://github.com/xrkk/Windows-MCP/tree/main/PLAN/2026.09/2026.09.15`。
- 写入位置规范化：GitHub 仓库 `xrkk/Windows-MCP`，分支 `main`，仓库内绝对目录 `/PLAN/2026.09/2026.09.15`。这是用户明确指定的远程仓库目录，不将其解释为本地磁盘路径。
- 本次唯一需求提炼文档：`PLAN/2026.09/2026.09.15/2026.09.15-01-需求提炼-Windows-MCP使用过程Token消耗优化.md`。
- 讨论依据：本会话上一轮 AI 对 Windows-MCP 的源码分析及优化建议。用户要求逐项评估并提问，不将这些建议直接视为已批准需求。
- 本轮工作范围：开展访谈并将实际问答和结论写入上述文档；未获得实施优化代码的授权。
- Q-001 补充的评估方向：减少 AI 调用工具的次数、减少 AI 做判断的次数，也属于本次 token 优化范围；不能只比较单次工具返回文本的长度。原始回答、采用边界和重新裁决条件见 Q-001 及累计有效结论。
- Q-022 修正的验收范围：只针对修改内容开展修改前后 token 消耗的局部、小范围对照，尽量排除模型智力及临场决策因素；不将完整自主任务的总消耗对照作为本轮必需验收。详见 C-023，不能用局部结果冒充全任务节省率。

### 启动输入 U-001

用户原文：

> 你一下子说的太多了.
> 读取 `https://github.com/xrkk/vpb/blob/main/%E8%AF%9D%E6%9C%AF/%E6%96%B9%E6%A1%88%E5%B7%A5%E4%BD%9C%E6%B5%81/02-%E8%AE%BF%E8%B0%88-GrillMe-%E9%80%9A%E7%94%A8%E7%89%88/00-%E5%B7%A5%E4%BD%9C%E6%B5%81%E5%85%A5%E5%8F%A3%E4%B8%8E%E4%BC%9A%E8%AF%9D%E5%88%9D%E5%A7%8B%E5%8C%96.md` 开启访谈工作流, 文档写入到目录 `https://github.com/xrkk/Windows-MCP/tree/main/PLAN/2026.09/2026.09.15`.
> 访谈内容: 减少 Windows-MCP 在使用过程中的 token 消耗
> 上面你说的内容, 逐项评估并提问.

本次交互采用逐项、单题的方式。目标和写入位置直接取自上述明确输入，未另行推定其他目标或目录。

### 来源与证据基线

- 本次读取的工作流入口：`xrkk/vpb` 的 `话术/方案工作流/02-访谈-GrillMe-通用版/00-工作流入口与会话初始化.md`；Git blob：`8a5bb1f6e0f50bd53510a5fefcc215a3aea18908`。
- 本次已读取的阶段文件：同目录 `01-开始访谈.md`；Git blob：`8341cdeee976217aa60840bd4b1957f26f1f8d69`。两者已核对为普通文件，身份和版本匹配。
- 上轮源码分析基线：`787385ec5f9688b0e24f9759b3d505d7084ba80b`。
- 本次初始化时读取的 `main` HEAD：`e5666bfe85ddb330f16322ba62f117223fb29494`。
- 两个基线的 `src` 子树均为 `1e50515bd20fd0370639bc89c9eaa167c8d14dfa`，初始化时所讨论源码与上轮已读取版本一致。文档写入基于当前分支，不回退分支。
- 初始化时已读取的仓库树中未发现 `CONTEXT-MAP.md`、`CONTEXT.md` 或项目 `docs/adr/` 记录；当时尚无经问答确认的新业务术语或架构决策，未创建空领域文档，也未修改工作流源文件。
- 现有判断来自静态源码；尚未取得用户实际任务日志、客户端计费数据或 Windows 运行对照结果。节省比例、成功率影响和最终优先级均未验证。
- 提出 Q-002 前重新读取了 `main` 的 `src/windows_mcp/tools/input.py` 第 275–335 行及 `src/windows_mcp/tools/multi.py` 第 60–130 行。文件 blob 分别仍为 `05c657d2301052417d3a5b9f29fe85013c2d157c`、`4f4c448fe46f3ee9e5be2869c0f2e06e0217f5d1`；与既有源码证据一致。全文回显直接来自输入参数，不是从界面重新读回的验证内容。
- Q-001 确认了保留已有逐词能力及成本评估方向，未确认新的项目术语定义；“AI 判断次数”的具体观测口径尚未确定。该保留现状的取舍不构成难以逆转的新架构决策，本轮没有需要新增的术语表条目或 ADR。
- Q-002 已确认取消输入全文回显及其边界，仅形成可逆的回执行为需求，未形成新的项目术语定义或难以逆转的架构决策；未创建领域文档、未变更实现代码。提出 Q-003 时，其快照返回方式仍待裁决，当时未作为项目共识或 ADR 写入。
- Q-003 用户已修正为“完整状态+变化内容”，本轮仅确认同次返回及工具内部承担对比的行为目标。比较基线、首次采集、基线失效、会话隔离和输出结构尚待细化，未以本次回答预先批准难以逆转的存储或协议设计。本轮未形成需要另建术语表或 ADR 的正式领域定义或架构取舍，仅更新本需求草稿，没有修改实现代码。
- Q-004 已确认不为省 token 强制分页，保留必要容量保护及不完整提示；具体阈值与超限处理仍待细化。该默认交付取舍未形成新的项目术语或难以逆转的架构决定，本轮仅更新本需求草稿。
- 提出 Q-005 前完整重读了 `main` 的 `src/windows_mcp/tools/scrape.py`，blob 仍为 `ad11580fc8f8279cc47475a33274e9cc61aa8175`，与已有证据一致：`use_sampling` 默认 True，在存在 `ctx` 时尝试 `ctx.sample`，异常回退抓取内容；`query` 仅用于这一模型调用中的定向提示，`max_tokens=2048` 限制摘要输出。尚未验证用户客户端是否支持或实际执行该额外模型调用。
- Q-005 已确认默认不做额外 AI 摘要，程序清理后直接返回内容，且不丢失任务所需内容、不引入补读。显式摘要、`query` 兼容及清理规则仍待细化；此项为尚未实施的默认行为取舍，没有新增项目术语定义或难以逆转的架构决定，本轮仅更新本需求草稿。
- 提出 Q-006 前重读了 `main` 的 `src/windows_mcp/tree/views.py` 第 20–180 行，blob 仍为 `8a49958f8a450ebfcab72ce39a36c4d6376b8927`。当前语义树输出包含层级连接线和缩进、控件名称与类型、坐标、动作提示及相关状态。本题仅评估排版精简，不把删除动作提示后由 AI 推断视为无损压缩，实际 token 与理解成本尚未测量。
- Q-007 已确认精简重复工具说明、保留必要用法，不增加查说明、猜参数、误用和重试。此项未改变工具集合或参数兼容，未形成新的项目术语定义或难以逆转的架构决定，本轮仅更新本需求草稿。
- 提出 Q-008 前完整重读 `main` 的 `src/windows_mcp/tools/__init__.py`，blob 为 `b4b5c95c08c01292594b5a823358d2ce26068794`，仍由 `register_all` 遍历 `_MODULES` 注册工具模块。注册行为不等于客户端一定把全部工具定义每轮原价发送给模型；本轮未核实用户客户端的工具注入、动态发现或缓存行为。
- Q-008 期间用户先要求查看全部工具，随后回答“Q8: A”。上轮展示清单时的代码核对基线为 `7aa5493fc8e33fc7e0da64ff4a0282d5094197b5`，与初始化基线比较只有本需求草稿发生变化。上轮未写回的清单及澄清过程已在本轮补记于 Q-008；本轮读取到的原文档 blob 为 `51deeedb368cc5fe38cb278797c4d7eb00e12b4e`，据此续写，未重建或重编号。
- Q-008 已确认默认全部提供已授权工具，保留既有禁用项，不新增面向 AI 的发现、启用或切换步骤。此项是保留现有可用方式的取舍，未形成新的项目术语定义或难以逆转的架构决定；本轮仅维护原需求草稿。
- 提出 Q-009 前重读 `main` 的 `src/windows_mcp/tools/snapshot.py` 第 1–52 行，blob 仍为 `5ac15b874ebed446d33921981ce9d50800c823f0`。其描述末尾要求行动前先调用 Snapshot；这是工具说明，不把它误写成所有其他工具都存在运行时强制门禁。Q-009 评估不依赖界面的任务是否还应具有这一前置要求。
- Q-009 已选择 A：不依赖界面且目标、参数明确的任务无需先取桌面快照；依赖界面定位的任务仍先观察，权限检查及必要结果验证保留。本项是可逆的调用前置行为取舍，未形成需同步的新术语或难以逆转的架构决定，只更新原需求草稿。
- 提出 Q-010 前完整重读 `main` 的 `src/windows_mcp/tools/snapshot.py`，blob 仍为 `5ac15b874ebed446d33921981ce9d50800c823f0`：Snapshot 默认 `use_vision=False`、`use_ui_tree=True`，可在同次调用附图；Screenshot 固定取图并跳过 UI 树。Q-010 的自动补图是待评估的新行为，不冒充现有功能；未验证客户端视觉能力或实际图像 token。
- Q-010 用户要求截图仅由 AI 显式调用，非必要不提供，不采纳自动补图或每次带图。该回答对截图场景明确了图像开销与额外调用的取舍，不能以减少调用为由自动传图；详见 C-011。此项未形成新的项目术语定义或难以逆转的架构决定，本轮只更新原需求草稿。
- 本轮根据会话中的用户原文修正 Q-001 引号前因历史写入累积的多余反斜杠，仅修复转义失真，不改变其回答或结论。
- Q-011 已选择 A：AI 已决定截图且已有可靠、足够的目标范围时，在同一次请求中优先指定该区域；未知范围或需全局信息时取完整画面，不为裁剪新增定位或重截步骤。此项未选定新的领域术语或难以逆转的架构，仅更新原需求草稿。
- 提出 Q-012 前重读 `main` 的 `src/windows_mcp/tools/_snapshot_helpers.py` 第 19–33 行，blob 仍为 `8fb77fad5caf6fcbf4599f33f6b96ed7a5de6791`：图像尺寸上限常量为 1920×1080，`WINDOWS_MCP_SCREENSHOT_SCALE` 未设置时取 1.0。结合已读取的 `desktop/service.py` 的尺寸约束与缩放实现，Q-012 讨论是否进一步默认缩小，不将“沿用现有设置”误称为始终返回原始分辨率；实际可读性和图像 token 仍未测量。
- Q-012 已选择 A：沿用现有尺寸上限和缩放设置，不默认进一步降低截图分辨率。此项保留已有尺寸策略，不形成新的项目术语或难以逆转的架构决定；本轮只更新原需求草稿。
- 提出 Q-013 前重读 `main` 的 `src/windows_mcp/tools/input.py` 第 420–560 行，blob 仍为 `05c657d2301052417d3a5b9f29fe85013c2d157c`。`WaitFor` 在单次调用内用无图 UI 树检查条件，匹配后返回文字回执，超时抛出错误；不把已有等待能力说成新增实现，也不把命中单个条件等同于完整业务成功。底层采集耗时及条件可靠性仍需运行验证。
- Q-013 已选择 A：条件明确且可可靠检测时，一次调用 WaitFor，由工具内部反复检查；满足或超时后返回，不无限等待、不自动截图。具体等待结束后的信息交付及故障恢复仍待细化。本项是已有能力的使用取舍，未形成新领域术语或难以逆转的架构决定。
- 提出 Q-014 前完整重读 `main` 的 `src/windows_mcp/tools/multi.py`，blob 仍为 `4f4c448fe46f3ee9e5be2869c0f2e06e0217f5d1`。已有 MultiEdit 和 MultiSelect 接收多个坐标或标签，在一次调用中执行多项输入或选择。本题只评估稳定、已知目标的批量使用，不将其描述为已经具备任意动作编排、事务回滚或自动识别所有布局变化的能力。
- Q-014 已选择 A：目标明确、布局稳定且中途无需新判断或确认时，优先已有批量工具；跳转、弹窗或结果依赖时仍分步。尚未选定部分完成和失败恢复的具体机制，未修改代码。Q-015 基于既有 WaitFor 源码证据评估等待结束后的状态交付，不将待选行为当作已实现。
- Q-015 已选择 A：WaitFor 结束时复用最后一次实际采集，同次返回等待结果、完整界面状态和工具内部计算的变化；不额外采集、不自动截图，失败或不完整如实标明。比较基线和异常交付协议仍待细化，未选定难以逆转的架构或新的领域术语。本轮重读入口、阶段 1 文件及原需求草稿后续写，未改变工作流源文件或实现代码。
- 提出 Q-016 前完整读取 `main` 的 `src/windows_mcp/tree/budget.py`，blob 为 `5847b22190f276ef70866f82e2c95daf1481b335`。现有默认采集元素上限为 500，可由 `WINDOWS_MCP_MAX_TREE_ELEMENTS` 配置；这是元素数约束，不是 token 上限。本题评估是否为省 token 调低默认值，不提前决定长值截断、范围筛选或超限恢复。
- Q-016 已选择 A：沿用现有 UI 树元素上限和用户配置，不为省 token 调低默认值；保留必要保护及截断提示。本项未形成新领域术语或难以逆转的架构决定，仅维护原需求草稿。
- 提出 Q-017 前重读 `main` 的 `src/windows_mcp/tree/views.py` 第 20–62 行，blob 仍为 `8a49958f8a450ebfcab72ce39a36c4d6376b8927`；`_node_meta_str` 直接格式化采集到的 value。结合既有名称格式化及元素预算证据，单个控件文本的长度与元素数量是不同约束。Q-017 单独评估长文本预览，不重开逐词定位和元素上限取舍。
- Q-017 已选择 A：容量允许时保留已采集的完整控件文本，不默认截短为预览；必要上限及不完整提示保留，见 C-018。此项为可逆的输出行为取舍，无新增领域术语或难以逆转的架构决定，未修改代码。
- 提出 Q-018 前完整读取 `main` 的 `src/windows_mcp/tools/filesystem.py`，blob `e98ba7fa45e9774d7832d61b39ae015d1f145983`：read 传递 offset/limit，list/search 使用文件名 pattern，list/search 不传递 limit。Q-018 评估利用已有、可靠的任务范围，而非默认分页或按未知范围反复试读。
- Q-018 已选择 A：所需行段或文件名条件已有可靠依据时，同次按范围读取；范围不明确时不猜、不拆成多次试读，不增加定位或补读，见 C-019。此项为已有能力的使用取舍，无新增领域术语或难以逆转的架构决定，未修改代码。
- 提出 Q-019 前完整读取 `main` 的 `src/windows_mcp/tools/_snapshot_helpers.py`，blob 仍为 `8fb77fad5caf6fcbf4599f33f6b96ed7a5de6791`：截图编码和返回均使用 PNG。尚无本次任务的图片压缩计费对照或已确认的传输瓶颈；Q-019 只评估是否把单纯压缩文件或更换编码纳入本轮，不重开截图触发、裁剪及分辨率取舍。
- Q-019 已选择 A：单纯压缩截图文件或更换编码暂不纳入本轮，保留现有编码；未确认传输瓶颈或实测 token 收益，见 C-020。Q-020 使用上轮已完整读取的同一辅助文件证据，单独评估未返回文本的内部生成清理；未修改代码，也未新增领域术语或难以逆转的架构决定。
- Q-020 已选择 A：仅减少未返回文本生成的内部性能清理暂不纳入本轮，见 C-021；不排除有证据减少超时或重试的修复。本轮读取原需求全文后续写，并按会话原文修正 Q-001 累积的引号转义，不改变原意。Q-021 单独评估文字 UI 树的已知范围策略，不把截图裁剪选择自动扩大为文字观察规则；未修改代码，未新增领域术语或 ADR。
- Q-021 已选择 A：文字 UI 树优先限定已有可靠、足够的目标区域，同次交付范围内完整状态与变化；未知范围或需全局信息时不缩小，不增加定位或补查，见 C-022。本轮只维护原需求草稿，无新增领域术语或难以逆转的架构决定。Q-022 评估是否把完整任务的优化前后实测对照列为验收要求，不假定已有运行数据或已经达到节省目标。
- Q-022 用户修正为针对修改点的局部、小范围 token 前后对照，尽量排除模型智力因素，见 C-023。原问答保留，不将该回答归为原 A 的完整任务实测或原 B 的不测 token；尚未执行测试。Q-023 继续明确完整状态加变化的比较对象，不重新询问已确认的交付方式。本轮仅更新需求草稿，未修改实现代码；本项是测试范围与方法约束，未形成新的项目领域术语或难以逆转的架构决定。
- 本轮核对 `main` 中本需求文件 blob 仍为 `01ac86c8a1c824893facdf28a029717174543a5b`，仓库记录停于 Q-023。会话中已经发生的 Q-023 澄清、Q-024 和 Q-025 及三次 A 回答在本轮补齐，不重新询问，不将先前口头“记录”当作已有 GitHub 写入证据。Q-023-A 作为 Q-023 的同题澄清别名保留，不另起序号；Q-024、Q-025 沿用已经展示的编号。
- Q-023 已确认运行时比较基线为同一会话最近一次成功返回给 AI 的可比状态，Q-024 确认只保留最近一次基线，Q-025 确认高层变化摘要；见 C-024、C-025、C-026。运行时基线不是修改前后测试的原版输出，不需要先运行原版。会话识别和交付成功的可观察判据尚待后续闭环，不以本轮术语确认假定客户端接收确认协议已经存在。
- 本轮读取的仓库树 `e3d7bb04e526de2296b93a58eee52a7e12391e19` 未截断，未发现 `CONTEXT-MAP.md`、`CONTEXT.md` 或 `/adr/` 条目；`src` 子树仍为 `1e50515bd20fd0370639bc89c9eaa167c8d14dfa`。已确认的“交付基线”和“变化摘要”将增量同步到项目根目录 `CONTEXT.md`，对应 C-024 至 C-026，不复制会谈状态。没有形成符合难以逆转条件的架构决定，不新建 ADR。Q-026 单独评估显式额外摘要能力是否保留，不重开默认关闭摘要的结论。

## 待逐项评估的候选事项

以下是上一轮分析及 Q-001 补充方向的议题索引，不是正式需求或已实施结果。仅明确标记为已确认的部分代表当前用户结论；其余继续逐项评估。

| 议题 | 待评估内容及不能遗漏的边界 | 当前进展 |
| --- | --- | --- |
| T-01 UI 树逐词展开与长文本 | 部分控件会生成逐词坐标节点，控件值又可能含相同文本。现确认保留直接逐词坐标、不调低默认元素上限；容量允许时保留已采集的完整控件文本，不默认截短为预览。必要容量保护和不完整提示保留；具体阈值、超限交付与重复表达的无损整理仍待闭环。 | Q-001 已选 B，见 C-001；Q-016 已选 A，见 C-017；Q-017 已选 A，见 C-018；尚未实施 |
| T-02 输入文本重复回显 | `Type` 和 `MultiEdit` 在结果中重复输入全文。改为同一次调用提供长度、目标和执行状态回执，保留必要错误；不因精简回执新增补查或确认步骤，执行回执不能冒充保存或提交成功的业务验证。 | Q-002 已选 A，结论见 C-003；尚未实施 |
| T-03 快照精简、范围与差分 | 完整当前状态与工具内部计算的高层变化摘要同次返回，不以变化替代完整状态，不让 AI 另行调用 diff 或自行拼接历史。文字 UI 树已有可靠、足够区域时优先限定范围，未知或需全局信息时不缩小，不增加定位或补查。默认比较同一会话最近一次成功返回给 AI 的可比状态，只保留最近一次交付基线，内部轮询不替换它。无可比基线明确说明，不报无变化。 | Q-003、Q-015、Q-021、Q-023 至 Q-025 已确认，见 C-004、C-016、C-022、C-024 至 C-026；交付判据、失效与范围可比性、节点匹配、必要全局元数据和预算顺序仍待闭环；尚未实施 |
| T-04 大输出预算与继续读取 | 覆盖 `PowerShell`、`FileSystem.read/list/search`、`Clipboard.get`。在容量允许时一次交付本次请求的完整结果，不为省 token 默认截断分页；保留必要容量保护，明确标记不完整。既有候选中的头尾预览、字段选择、完整结果保留、继续读取、字符与 token 预算差异，以及各工具现有不同上限，仍需逐项取舍，不因本题自动引入。 | Q-004 已选 A，结论见 C-005；超限后的具体处理、阈值和各工具差异尚未定案，尚未实施 |
| T-05 Scrape 摘要及回退 | 现有默认 sampling 会把原文交给另一轮模型处理，2048 限制的是摘要输出；失败会回退原文，query 的定向作用位于摘要阶段。用户确认改为默认不做额外 AI 摘要，程序清理后直接返回，不能丢失任务所需内容或增加补读。query 语义、显式请求摘要、清理及回退规则仍需处理，不能静默忽略参数；总成本仍包含主模型读取正文。 | Q-005 已选 A，结论见 C-006；Q-026 待回答：保留显式开启还是彻底移除额外摘要；具体清理与兼容边界尚未定案，尚未实施 |
| T-06 UI 树表示格式 | 原候选包含减少连接符、缩进和默认动作说明。现按 C-002 仅精简冗余排版，保留清晰层级、名称、类型、逐词坐标、动作提示及相关状态，不让 AI 补查或推断被省略信息。若未来引入编号，必须与动作解析对应；具体文本或 JSON 格式及实际收益不在本题提前定案。 | Q-006 已选 A，结论见 C-007；只精简排版，不删操作信息，尚未实施 |
| T-07 工具描述与工具集合 | 精简不提供新信息的重复说明，保留用途、参数约束、适用条件、安全提示及必要关键词和示例；默认全部提供已授权工具，不新增查找、启用或切换步骤，尊重既有禁用项。不依赖界面且目标、参数明确的任务不必先取 Snapshot；依赖 UI 定位时仍先观察，权限和必要验证不变。保留必要的 bool/string、list/string 客户端兼容，避免增加失败重试。工具定义和缓存收益取决于客户端，不预设全部定义每轮原价计费。 | Q-007 已选 A，结论见 C-008；Q-008 已选 A，结论见 C-009；Q-009 已选 A，结论见 C-010；尚未实施 |
| T-08 已有文字/视觉路径 | 截图仅在 AI 显式请求时提供，非必要不提供；不因 UI 树失败、为空或疑似缺少视觉信息而自动附图，也不每次同时返回 UI 树与截图。UI 树失败如实返回文字状态和错误。保留显式取图能力，不将完整状态加变化解释为必须附图。 | Q-010 用户修正已确认，结论见 C-011；原自动补图建议不采纳，尚未实施 |
| T-09 已有区域、显示器和缩放配置 | AI 已决定截图且已有可靠、足够的范围时优先局部截图；未知范围或需全局信息时取完整画面，不为裁剪增加定位或重截步骤。沿用现有尺寸上限和缩放设置，不默认进一步降低分辨率。沿用现有 UI 树元素上限和用户配置，不为省 token 调低默认值；上轮的 0.75、150 均不作为新默认值。其他范围边界仍考虑小字、坐标换算、漏元素和重试，不按像素比例承诺 token 比例。 | Q-011 已选 A，见 C-012；Q-012 已选 A，见 C-013；Q-016 已选 A，见 C-017；Q-021 已选 A，文字 UI 树范围见 C-022；其他范围和超限边界尚待评估 |
| T-10 等待与批量交互 | 条件明确且能可靠检测时优先一次调用已有 WaitFor，在工具内检查并于满足或超时后返回，不无限等待、不自动截图。多个目标已知、布局稳定且中途无需新判断或确认时优先 MultiEdit、MultiSelect；页面跳转、弹窗、布局变化和结果依赖时仍分步处理。WaitFor 结束时复用最后一次实际采集，同次交付等待结果、完整状态与变化，不额外采集，失败或不完整如实标明。 | Q-013 至 Q-015 已选 A，见 C-014 至 C-016；Q-023、Q-024 确认内部轮询不更新对外交付基线，只保留最近一次基线，见 C-024、C-025；变化粒度见 C-026；部分完成和故障恢复仍待细化，尚未实施 |
| T-11 已有文件读取筛选 | 已知所需行段或文件名条件且范围可靠、足够时，优先同次利用 offset/limit 或 pattern 取得所需结果；未知范围不猜测、不拆成多次试读，不为筛选增加定位或补读。现有 list/search 不使用 limit，不把文件名筛选说成正文检索；所需范围内仍遵守容量允许时一次完整交付。 | Q-018 已选 A，见 C-019；不强制分页及必要容量保护不变；尚未实施 |
| T-12 图像文件压缩的实际收益 | 单纯压缩截图文件或更换编码暂不纳入本轮，保留现有编码；不把文件字节减少直接计作 token 节省。截图显式请求、已知可靠范围局部优先及不进一步默认降分辨率的结论不变。 | Q-019 已选 A，见 C-020；出现明确传输瓶颈或实测收益时再裁决 |
| T-13 内部重复渲染的实际收益 | 仅减少未返回 UI 文本生成和处理的内部性能清理暂不纳入本轮，不将程序提速计作直接 token 节省；不排除有证据减少超时或重试的修复，也不禁止实现已选需求所必需的内部重构。 | Q-020 已选 A，见 C-021；出现实测间接收益或用户扩展范围时再裁决 |
| T-14 验收口径 | 本轮针对修改点做局部、小范围的 token 前后对照，固定样本、输入、初始状态及必要的小操作链，尽量排除模型智力和自由决策影响；不要求完整自主任务对照。分开记录文本 token 差额、工具及额外模型调用差额、功能与信息完整性结果。局部涉及的新增和删减均计入，不把局部结果推成全任务节省率，不虚构隐藏判断的 token 成本。 | Q-022 用户修正已确认，见 C-023；取代原候选的完整任务实测要求，不等于取消 token 测试。具体样本、计数器、图像计量及阈值待细化；尚未执行 |
| T-15 取舍与顺序 | 上轮提出先处理全文回显、长输出预算和逐词展开，再考虑目标筛选及差分快照，仅为候选顺序。最终保留、简化、删除、不引入和实施顺序尚未裁决。 | Q-001 保留直接逐词输出；Q-002 取消输入全文回显；Q-003 采用完整状态加工具内计算的变化内容，不采用仅差分返回；Q-004 不为省 token 强制分页；Q-005 默认不做额外 AI 摘要；Q-006 只精简 UI 树排版；Q-007 精简重复工具说明；Q-008 默认全部提供已授权工具；Q-009 非界面任务取消无关快照前置；Q-010 截图仅由 AI 显式请求；Q-011 已知可靠范围时优先局部截图；Q-012 不默认进一步降分辨率；Q-013 条件明确时优先工具内部等待；Q-014 已知稳定目标优先批量；Q-015 WaitFor 同次交付末次状态与变化；Q-016 不调低默认元素上限；Q-017 不默认截短已采集长文本；Q-018 已知可靠文件范围优先同次筛选；Q-019 暂不纳入单纯图片文件压缩；Q-020 暂不纳入未返回文本的内部性能清理；Q-021 文字 UI 树优先限定已知可靠范围；Q-022 采用修改点局部 token 对照；Q-023 采用上次成功交付的可比状态；Q-024 只保留最近一次基线；Q-025 高层变化摘要；其余取舍及实施顺序待评估 |
| T-16 减少 AI 调用与判断 | 将减少 AI 发起的工具调用、减少 AI 为下一步选择/补查/确认而参与判断，作为 token 优化方向。逐项区分 AI 往返与工具内部确定性处理，不把更短回包自动视为更低全过程成本；不预先批准新增自动执行或跳过必要验证。 | 方向由 Q-001 用户主动补充并确认；Q-003 确认工具内部计算并随完整快照给出变化；Q-005 确认取消默认额外模型摘要；Q-008 不新增工具发现或加载步骤；Q-009 取消非界面任务无关的快照前置；Q-013 确认重复等待检查留在工具内部；Q-014 确认符合前提时优先批量；Q-015 确认直接复用等待末次采集减少补取；截图场景须遵守 Q-010 的显式请求门槛，不能为省一次调用自动附图；Q-022 要求局部固定流程测试，不用自主模型推理差异衡量修改收益；其他机制仍待评估 |

### 源码定位

以下链接固定到本次源码基线，供后续逐项核实，不以链接代替最终需求正文。

- T-01、T-06：[tree/service.py](https://github.com/xrkk/Windows-MCP/blob/e5666bfe85ddb330f16322ba62f117223fb29494/src/windows_mcp/tree/service.py) 的 `tree_traversal`、`_append_word_nodes`；[tree/views.py](https://github.com/xrkk/Windows-MCP/blob/e5666bfe85ddb330f16322ba62f117223fb29494/src/windows_mcp/tree/views.py)；[tree/budget.py](https://github.com/xrkk/Windows-MCP/blob/e5666bfe85ddb330f16322ba62f117223fb29494/src/windows_mcp/tree/budget.py)。
- T-02、T-10：[tools/input.py](https://github.com/xrkk/Windows-MCP/blob/e5666bfe85ddb330f16322ba62f117223fb29494/src/windows_mcp/tools/input.py) 的 `type_tool`、`wait_for_tool`；[tools/multi.py](https://github.com/xrkk/Windows-MCP/blob/e5666bfe85ddb330f16322ba62f117223fb29494/src/windows_mcp/tools/multi.py)。
- T-03、T-08、T-09、T-12、T-13、T-14：[tools/_snapshot_helpers.py](https://github.com/xrkk/Windows-MCP/blob/e5666bfe85ddb330f16322ba62f117223fb29494/src/windows_mcp/tools/_snapshot_helpers.py)；[tools/snapshot.py](https://github.com/xrkk/Windows-MCP/blob/e5666bfe85ddb330f16322ba62f117223fb29494/src/windows_mcp/tools/snapshot.py)；[desktop/service.py](https://github.com/xrkk/Windows-MCP/blob/e5666bfe85ddb330f16322ba62f117223fb29494/src/windows_mcp/desktop/service.py) 的 `get_state`。模型计费和客户端行为仍需按实际使用环境核实。
- T-04、T-11：[tools/shell.py](https://github.com/xrkk/Windows-MCP/blob/e5666bfe85ddb330f16322ba62f117223fb29494/src/windows_mcp/tools/shell.py)；[powershell/service.py](https://github.com/xrkk/Windows-MCP/blob/e5666bfe85ddb330f16322ba62f117223fb29494/src/windows_mcp/powershell/service.py)；[tools/filesystem.py](https://github.com/xrkk/Windows-MCP/blob/e5666bfe85ddb330f16322ba62f117223fb29494/src/windows_mcp/tools/filesystem.py)；[filesystem/service.py](https://github.com/xrkk/Windows-MCP/blob/e5666bfe85ddb330f16322ba62f117223fb29494/src/windows_mcp/filesystem/service.py)；[filesystem/views.py](https://github.com/xrkk/Windows-MCP/blob/e5666bfe85ddb330f16322ba62f117223fb29494/src/windows_mcp/filesystem/views.py)；[tools/clipboard.py](https://github.com/xrkk/Windows-MCP/blob/e5666bfe85ddb330f16322ba62f117223fb29494/src/windows_mcp/tools/clipboard.py)。
- T-05：[tools/scrape.py](https://github.com/xrkk/Windows-MCP/blob/e5666bfe85ddb330f16322ba62f117223fb29494/src/windows_mcp/tools/scrape.py)。
- T-07：[tools/__init__.py](https://github.com/xrkk/Windows-MCP/blob/e5666bfe85ddb330f16322ba62f117223fb29494/src/windows_mcp/tools/__init__.py) 的 `register_all`，以及各工具的公开描述和参数。
- T-16：需求来源为 Q-001、Q-003 用户原始回答；可复用能力的静态事实关联 T-10，快照变化返回的新增目标见 C-004，其余机制尚未选定。

## 累计有效结论

### C-001｜保留直接提供逐词坐标的现有能力（来源：Q-001）

- 当前有效结论：用户选择 B。在当前已经支持逐词定位的 UI 树采集路径中继续直接返回逐词坐标，不将其改成默认不返回、再由 AI 判断是否需要并额外调用工具获取，也不取消该能力。
- 适用前提和边界：本结论针对 Q-001 的逐词定位取舍，不要求原本跳过 UI 树的截图路径新增逐词提取，也不将尚未讨论的长值预算、输出格式或采集范围一并定案。
- 采用依据：用户明确认为，按需获取会增加一次工具调用及 AI 判断，该成本比本项拟省的少量返回 token 更重要。因此不采纳当时推荐的 A。
- 重新裁决触发条件：用户改变该优先级，或后续出现能够在不新增 AI 调用和判断负担的前提下保留同等可用信息的替代方案，再提交用户裁决；不能自行以回包变短推翻本结论。

### C-002｜把减少 AI 工具调用和判断纳入优化目标（来源：Q-001 补充意见）

- 当前有效结论：减少 AI 调用工具的次数、减少 AI 做判断的次数，是本次减少 token 消耗的明确优化方向。评估各项时必须同时考虑是否让 AI 增加选择、补查、确认或重试，不能只按单次返回文本减少量判断收益。
- 适用前提和边界：用户在本项明确拒绝用一次额外 AI 往返换取少量逐词输出节省；本轮没有给出所有任务的绝对调用次数上限，也没有授权为了减少次数而省略必要验证或直接实施自动执行。判断负担的验收按 C-023 采用小范围固定流程和可观察的调用边界，不数模型隐藏推理、不虚构每次判断对应的 token；各候选机制仍待逐项确定。
- 采用依据：Q-001 用户主动提出上述两个方向，并说明拒绝按需逐词读取的原因。把这一意见作为本次需求取舍依据，不将“任意一次调用必然比任意文本量更贵”记录为已测量的普遍事实。
- 重新裁决触发条件：具体方案减少一类开销却增加另一类开销、影响成功率或关键验证，或后续实际任务数据与当前预期不一致时，按具体场景重新裁决，不仅凭回包字数自动通过。

### C-003｜取消输入全文回显且不增加补查（来源：Q-002）

- 当前有效结论：用户选择 A。`Type` / `MultiEdit` 输入完成后的回执不再重复返回输入全文；同一次调用仍返回执行状态、目标、字数和必要错误，下一步所需信息一次给齐，不因这一精简引入额外补查步骤或让 AI 再判断是否需要补查。
- 适用前提和边界：调用方已拥有传入的文本，所删除的是输入参数的重复回显，而不是从界面实际读回的验证结果。执行状态只表达实际完成的动作及已知错误，不冒充保存成功、提交成功或界面内容验证成功；现有必要业务验证不因本项被省略。不据此引入需 AI 选择的回执模式、额外摘要调用或全文补查机制，其他工具的读取正文也不属于本项删减范围。
- 采用依据：Q-002 用户原始回答为“A”，明确接受该题的选项 A 及其不增加补查和判断的前提。源码中的全文回执直接来自输入参数，删除这段重复不等于删除独立验证证据。实际全过程 token 节省量仍待测量。
- 重新裁决触发条件：发现客户端实际依赖回显全文、精简后缺少下一步必要信息，或因此增加补查、判断、失败重试或误报成功时，提交用户重新裁决，不静默增加补查以维持表面上的短回执。

### C-004｜同次返回完整状态与工具内计算的变化内容（来源：Q-003 用户修正）

- 当前有效结论：快照在同一次工具调用中返回当前观察范围内的完整状态，并附带工具内部计算出的变化内容。变化内容是补充信息，不代替完整当前状态；不要求 AI 结合旧快照拼接当前状态，也不让 AI 另行调用工具执行 diff 或自行逐项比较。对比工作由工具内部的确定性处理承担，不为计算变化增加另一轮模型调用。
- 适用前提和边界：“完整”沿用 Q-003 的范围，即本次观察范围和采集能力内无需历史差分还原的当前状态，不等于强制采集全部屏幕、取消既有采集限制或必须附图；C-001 的直接逐词坐标能力仍保留。接受完整状态之外附加变化内容的输出成本，目标是减少 AI 为识别变化付出的调用和判断。该回答未授权每次动作后自动触发新快照，也没有扩大到文件全文或其他工具结果策略。
- 已细化与待细化事项：C-024 确认默认相对同一会话最近一次成功返回给 AI 的可比状态；C-025 确认只保留最近一次交付基线；C-026 确认默认高层变化摘要。首次或无可比基线须明确说明，不能报无变化。会话识别、交付判据、范围及部分状态的可比性、节点匹配和具体字段仍待闭环，不能让 AI 另调 diff，也不预先批准未选择的持久化或接收确认协议。
- 采用依据：用户原始回答明确要求“完整状态+变化内容”，并说明要省去 AI 调用工具执行 diff 的开销。这是在原选项 A 的完整状态之上增加变化结果的自定义修正，不等同于原 A 的仅完整状态，也不是原 B 的仅差分返回。此前二选一没有覆盖这一组合，不继续沿用该二分作为限制。
- 收益证据边界：这是用户要求达到的行为目标；未取得实际任务调用记录，不宣称原系统每次观察都必然多一次 diff 调用，也不虚报已节省一次调用或具体 token 数量。按 C-023 在固定的小范围对照中同时记录附加变化文本和相关调用的增减；对比正确性用固定状态验证，不把可能减轻的模型判断负担换算成假定 token。
- 重新裁决触发条件：变化计算不可靠、与完整状态矛盾、基线不可比却误报变化，或实现需要新增 AI 对比调用、显著增加整体成本或失败重试时，按具体影响提交用户裁决；不自行改为只返回变化或删除完整状态。

### C-005｜不为省 token 强制分页，保留必要容量保护（来源：Q-004）

- 当前有效结论：用户选择 A。文件、日志、命令等结果在容量允许时，一次返回本次请求的完整内容；不为了缩短单次回包而默认截断分页、让 AI 多次补读或判断是否补读。超出必要安全上限时明确提示结果不完整，不静默截断后冒充完整结果。
- 适用前提和边界：“完整”针对本次请求已经确定的范围，仍尊重调用者明确指定的筛选、偏移和读取数量；不等于无条件读整个文件或取消既有安全保护。本结论不把现有各工具不同的上限统一为无限输出，也不预先决定具体阈值、超限后的内容保存、继续读取或响应协议；这些边界仍待细化。
- 采用依据：Q-004 用户原始回答为“A”，接受不强制分页及保留安全上限的组合。它沿用 C-002 的全过程成本取舍，避免本可一次交付的信息被人为拆成多轮调用。未取得实测数据，不宣称所有长输出一次返回在所有客户端都更便宜。
- 重新裁决触发条件：实际客户端无法承载正常结果、出现工具或上下文超限，或某类输出的一次交付导致成本、成功率或可靠性与目标冲突时，按该场景重新裁决；不能据此自行恢复对全部正常结果的默认分页，也不能取消必要保护。

### C-006｜网页抓取默认不做额外 AI 摘要（来源：Q-005）

- 当前有效结论：用户选择 A。网页抓取后默认不再调用额外模型生成摘要，改为由程序清理网页杂项后直接返回内容；清理不能丢失任务所需内容，也不能因此引入补读或额外判断。
- 适用前提和边界：这是默认链路的行为取舍，不等于禁止显式请求摘要、删除 `use_sampling` 参数或引入需 AI 再选择的新模式。程序清理应保留任务所需正文、数据和结构，具体规则尚待细化；`query` 的现有定向提取语义及兼容方式须继续明确，不能静默忽略参数或伪装已完成定向提取。容量不足仍遵守 C-005 的必要保护及不完整提示。
- 采用依据：Q-005 用户原始回答为“A”，接受少一次默认模型处理、可能返回更多文本的取舍。应把主模型读取较长内容的成本与被省去的摘要输入、输出一并评估，实际净节省尚未测量；不宣称用户客户端此前必然执行过该摘要。
- 重新裁决触发条件：程序清理遗漏任务内容、`query` 或显式调用兼容发生冲突、因此增加补读和判断，或全过程成本与成功率不符合目标时，按具体场景重新裁决，不静默恢复默认额外模型调用。

### C-007｜只精简 UI 树排版，保留操作信息（来源：Q-006）

- 当前有效结论：用户选择 A。减少 UI 树中的多余空格和装饰符，仍清楚表达层级、名称、控件类型、坐标、动作提示及相关状态；保留 C-001 的直接逐词定位和 C-004 的完整状态加变化内容，不以删减操作信息换取更短输出。
- 适用前提和边界：AI 理解起来不更费劲，不增加工具调用、补查、解码或模式选择。承载层级关系的连接符不能简单当作装饰删除，不能让 AI 猜动作、层级或被省略的值与状态。具体序列化格式、编号及兼容迁移未在本题选定，也未授权实施代码。
- 采用依据：Q-006 用户原始回答为“A.”，接受只精简排版及不增加理解负担的前提。实际 token、理解准确性和消费者兼容性仍需对照验证，不把字符减少直接当作净收益。
- 重新裁决触发条件：格式精简导致层级或动作歧义、信息丢失、客户端不兼容，或增加判断、调用和重试时重新裁决；不能为了维持压缩而要求 AI 自行补全信息。

### C-008｜精简重复工具说明，保留必要用法（来源：Q-007）

- 当前有效结论：用户选择 A。工具说明只去掉不提供新信息的重复内容，保留用途、参数约束、适用条件和安全提示；选用工具和填写参数所需信息仍直接可得，不让 AI 额外查说明或猜参数，不以增加误用、判断或重试为代价。
- 适用前提和边界：必要关键词、示例和安全提示不能机械删除，也不能转移到需要额外发现或读取的文档。此次仅裁决文案精简，不改变参数类型、默认值、兼容处理和能力，不自动批准隐藏工具、按需加载或更改 Snapshot 的调用前置要求。具体删改内容仍需核实用法与兼容性。
- 采用依据：Q-007 用户原始回答为“A”，接受精简重复说明及不增加理解和调用负担的组合。实际收益取决于客户端发送给模型的工具说明及实际调用表现，不将源文件字数下降当作等比例 token 节省。
- 重新裁决触发条件：精简导致用途或参数歧义、必要信息缺失、安全提示丢失、调用成功率下降，或增加查说明、猜测和重试时重新裁决，不为追求更短说明而让 AI 补全被删信息。

### C-009｜默认全部提供已授权工具，不新增发现和加载步骤（来源：Q-008）

- 当前有效结论：用户在查看全部工具清单后选择 A。项目默认提供全部已授权工具，让 AI 直接选用，不为省工具说明的 token 而新增查找、启用、切换或运行时按需加载步骤；接受必要工具说明的开销，不采用本轮候选的预先固定精简或动态加载方案。
- 适用前提和边界：尊重用户已有权限、安全配置及客户端禁用项，不自动重新启用被禁用工具，也不把“全部提供”解释为全部执行。保留 C-008 对重复说明的精简；本项不改变工具内部延迟初始化、参数兼容或既有动作权限，不强制客户端每轮发送所有定义，不预设工具定义的缓存及计费方式。用户未声明任务类型固定，也未确认排除某个具体工具。
- 采用依据：用户先要求“把所有工具列出来我看一下”，在获得 20 个工具的用途清单后明确回答“Q8: A”。该选择与 C-002 的减少 AI 发现、判断及额外调用方向一致；不据此虚报实际已减少的 token 或加载次数。
- 重新裁决触发条件：用户明确改变需要的工具范围或权限，或实际客户端的工具承载能力、调用准确性及总成本出现与本项取舍冲突的证据时，按具体问题重新裁决；不由程序或 AI 静默改为缩减集合或另行加载。

### C-010｜非界面任务取消无关快照前置（来源：Q-009）

- 当前有效结论：用户选择 A。文件路径或命令等目标、参数及执行条件已明确，并且任务不依赖当前界面时，直接使用相应工具，不先获取桌面快照；需要界面定位时仍先观察。必要权限检查和结果验证不被省略。
- 适用前提和边界：不依赖窗口、焦点、坐标或视觉状态的任务才适用；不能仅凭工具名是 PowerShell 或 FileSystem 就推定独立于界面。不新增“是否需要快照”的工具或模型裁判，不授权盲点坐标、失效标签、跳过权限或把动作完成冒充业务成功。本题不改变 C-004 的快照内容，也不决定 UI 动作后的观察频率。
- 采用依据：Q-009 用户原始回答为“A”，接受取消无关前置调用、保留必要观察和验证的组合。静态证据是 Snapshot 的说明文字，不声称此前所有非界面任务实际都多一次快照；收益仍需按真实调用记录比较。
- 重新裁决触发条件：发现任务实际依赖 UI 状态、取消前置观察导致误操作或重试，或实现新增了选择和裁判调用时，按具体场景重新裁决，不将必要观察一概删除。

### C-011｜截图仅由 AI 显式请求，非必要不提供（来源：Q-010 用户修正）

- 当前有效结论：需要截图时由 AI 显式调用请求；未明确请求图片时不提供截图。不采用 UI 树失败或为空时自动附图的候选，也不默认每次带图。UI 树失败、为空或不完整时如实返回文字状态及必要错误，不把失败自动转为图像输出。
- 适用前提和边界：这是截图的提供门槛，不删除已有显式取图能力，也不将 C-004 的“完整状态+变化内容”解释为必须附图。AI 已明确需要看图时可直接请求图片，不强迫先做一次纯文本观察；本题不决定工具合并、拆分或删除现有显式参数。是否取图仍由 AI 决定，不再加入额外模型裁判。截图范围、分辨率及坐标处理另行评估。
- 采用依据：用户明确表示“要获取截图时由 AI 显式调用, 因为 AI 看图比较费 token, 所以非必要不提供”。这对应原选项 C 的显式控制方向，并明确否定原推荐 A 的自动补图。用户在图像场景选择避免非必要视觉开销，即使确有需要时可能再取图；不能以 C-002 的减少调用目标覆盖这一具体取舍。
- 收益边界：保留用户对图像成本的定性取舍，不将所有图像都比任意长度文字更贵记录为定量事实；未测量实际图像 token、补图次数或任务净收益。失败回执不冒充成功取得完整 UI 树，必要的操作安全与验证边界不变。
- 重新裁决触发条件：用户改变截图触发要求，或具体任务出现不可接受的反复取图、信息不足、成本或成功率问题时，再由用户裁决；不得自行恢复自动附图、每次带图或静默视觉降级。

### C-012｜已知可靠范围时优先局部截图，不新增定位步骤（来源：Q-011）

- 当前有效结论：用户选择 A。AI 已明确决定截图，并且已有可靠且足够覆盖任务所需内容的目标区域时，在同一次截图请求中直接指定该区域；范围未知、失效或需要全局信息时取完整画面，不为裁剪额外调用工具取坐标或重截。
- 适用前提和边界：沿用 C-011 的显式取图门槛，尊重调用者指定的范围；不猜测区域，不为了减小画面而丢掉任务需要的周边信息。本项仅确认截图范围策略，不自动收窄所有 UI 树观察，不改变截图分辨率、元素预算或已有必要校验，不引入智能裁剪模型、自动窗口识别或分块多图机制。
- 采用依据：Q-011 用户原始回答为“A”，接受利用已有可靠范围减少无关画面、同时避免新增定位和重截的组合。实际图像 token、覆盖完整性和坐标可用性仍需验证，不承诺按面积同比节省。
- 重新裁决触发条件：已有范围不再可靠、裁剪遗漏必要信息、坐标映射有误，或增加定位、重截和判断负担时，按具体场景重新裁决；不得为坚持局部截图而额外搜集范围或悄悄扩大截图触发条件。

### C-013｜不默认进一步降低截图分辨率（来源：Q-012）

- 当前有效结论：用户选择 A。沿用现有图像尺寸上限和缩放设置，不为了省 token 默认进一步缩小截图；保留用户已有缩放配置，不采用此前提出的 0.75 作为新的默认值。
- 适用前提和边界：现有截图能够满足任务可读性；沿用现有设置不等于取消已有尺寸上限、始终返回原始分辨率或自动提高分辨率。C-011 的显式取图和 C-012 的已知可靠范围局部优先继续适用。本项不决定图片编码、UI 树元素上限，也不新增清晰度判断模型或多档图片输出。
- 采用依据：Q-012 用户原始回答为“A”，接受不额外降低清晰度、避免由此增加重截和判断的取舍。实际图像 token 和可读性尚未测量，不宣称保留现有策略必然适合所有任务。
- 重新裁决触发条件：现有设置不能满足文字或细节辨认、坐标使用或容量要求，或出现与目标冲突的重截和整体成本时，针对具体场景重新裁决，不自行改成统一低分辨率。

### C-014｜条件明确时优先工具内部等待和检查（来源：Q-013）

- 当前有效结论：用户选择 A。等待条件明确且可由工具可靠检测时，一次调用已有 WaitFor，由工具内部反复检查，条件满足或超时后返回；不让 AI 反复调用 Wait 和 Snapshot 再判断是否就绪。超时明确报错，不无限等待，不自动截图，也不增加额外模型裁判。
- 适用前提和边界：无法明确或可靠表达的等待条件不强行套用；命中一个等待条件不等于完整业务成功，必要权限检查、实际操作验证及 C-011 的显式取图门槛仍保留。既有 Wait 和 WaitFor 均保留，不据此删除工具、自动重试有副作用的操作或实施代码。等待结束时的同次状态交付已由 Q-015 确认，见 C-016；变化基线衔接、采集异常与超时恢复的具体机制仍待细化。
- 采用依据：Q-013 用户原始回答为“A”，接受把重复检查留在工具内部以减少 AI 往返及“好了没有”的判断，同时保留超时和不自动截图的边界。源码已有这一路径；实际耗时、条件可靠性、减少的调用及全过程 token 仍需对照验证，不宣称已完成底层可靠性整改。
- 重新裁决触发条件：条件误判、采集阻塞导致不能及时超时、无效重复等待、错误状态不清楚，或实际流程增加 AI 判断、调用或重试时，按具体场景重新裁决；不以等待命中冒充业务成功，也不自行恢复自动截图。

### C-015｜已知稳定目标优先批量操作（来源：Q-014）

- 当前有效结论：用户选择 A。多个输入框或选择项的目标已明确、布局稳定，且中途无需新的判断或确认时，优先一次调用已有 MultiEdit / MultiSelect；遇到页面跳转、弹窗、布局变化或需要根据前一步结果决定下一步时，仍分步处理。
- 适用前提和边界：只合并已有可靠目标的同类操作，不扩大为任意动作编排、自动提交或绕过权限与必要确认。保留必要验证、真实失败信息、不自动截图及 C-003 的取消输入全文回显；不假定事务回滚或整批成功，不默认失败后整批重做。部分完成、失败停止和恢复表达仍待细化。
- 采用依据：Q-014 用户原始回答为“A”，接受稳定场景减少逐项 AI 往返的取舍。底层点击或输入仍需实际执行，不能把一次工具调用误当作一次底层动作；实际成功率、重试和全过程 token 未测量。
- 重新裁决触发条件：目标或布局不可靠、中途出现新判断或确认、部分失败无法辨明，或批量增加误操作、重试和总体成本时，按场景重新裁决，不为减少调用强行批量。

### C-016｜WaitFor 同次交付末次采集的完整状态与变化（来源：Q-015）

- 当前有效结论：用户选择 A。WaitFor 等待结束后，复用等待过程最后一次实际采集，在同一次工具调用中连同等待结果返回完整界面状态和工具内部确定性计算的变化内容；不为了组织回执再额外采集，也不自动截图。避免 AI 仅为取得同一份已采集状态再调用 Snapshot，不要求另行调用 diff 或拼接历史状态。
- 适用前提和边界：“完整”限于末次实际采集的范围和能力；保留既有逐词定位和操作信息，不伪造未采到的元素，不把旧状态说成返回瞬间的新采集。仅交付末次可用采集，不回传每轮内部检查的全部状态。采集失败、为空或不完整必须如实标明；没有可用状态时明确不可用，不能为凑齐结果额外采集。等待满足、超时及采集异常分别如实表达，附带状态不等于等待成功或全部业务验证通过。显式取图门槛、权限和必要验证不变，不新增模型裁判、不自动执行下一步、不扩大为每个动作后强制获取快照。
- 已细化与待细化事项：Q-023、Q-024 已确认默认交付基线不被内部轮询覆盖，只保留最近一次成功交付的基线；Q-025 已确认高层变化摘要。范围变化、会话识别、交付失败、部分采集、超时和采集异常的具体协议仍待闭环。无可比基线不能误报无变化，不能退回要求 AI 自行 diff；不由本题预先批准持久化架构或客户端额外确认调用。
- 采用依据：Q-015 用户原始回答为“A”，明确接受同次返回及不额外采集、不自动截图、失败或不完整如实标明的前提。现有 WaitFor 已在内部获取状态而仅返回文字回执，本项要求改变返回行为，尚未实施；应同时统计附带状态的文本成本和实际减少的补取、对比及判断，不宣称所有等待必然省一次调用。
- 重新裁决触发条件：末次采集不可靠、状态与变化矛盾、错误状态被隐藏、实现需要额外采集或自动截图，或实际增加补查、误操作、重试和总体成本时，按具体场景重新裁决；不能静默改成仅短回执或伪装完整成功。

### C-017｜不为省 token 调低 UI 树默认元素上限（来源：Q-016）

- 当前有效结论：用户选择 A。沿用现有 UI 树采集元素数量上限和用户配置，不为缩短返回而调低默认上限、使采集更早截断；保留必要容量保护及截断提示，不采用此前提出的 150 作为新默认值。
- 适用前提和边界：现有上限能满足常用任务及容量要求；保留上限不等于保证采集完整，触及限制时仍明确标记不完整。该选择不取消上限、不强制覆盖用户设置，也不批准无限采集或自动调参；逐词定位、完整状态加变化和截图显式请求门槛保持不变。长名称和内容的默认交付方式已由 Q-017 确认，见 C-018；具体文本容量阈值、区域筛选顺序及超限恢复仍另行评估。
- 采用依据：Q-016 用户原始回答为“A”，接受避免过早截断导致补查和判断的取舍。元素数量不是 token 数；实际任务覆盖、调用、重试和总成本仍需验证。
- 重新裁决触发条件：现有上限本身导致目标遗漏、条件检测受限、容量或性能问题，或用户明确改变配置需求时，按具体场景重新裁决；不据此自行统一降低默认值或取消保护。

### C-018｜不默认截短已采集的控件长文本（来源：Q-017）

- 当前有效结论：用户选择 A。单个控件的名称或内容很长时，容量允许则返回已采集的完整文本，不默认只给预览，不让 AI 仅为恢复被省略的内容再补读；超出必要上限时明确标记不完整。
- 适用前提和边界：结果能够在容量限制内正常交付；不取消必要保护，不额外采集原本未取得的内容，不把采集片段冒充来源全文。保留逐词坐标、清楚的操作信息和完整状态加变化，不以词节点代替正文、迫使 AI 重拼，不新增摘要模型或自动截图。具体容量阈值、超限交付与重复表达的无损整理仍待闭环。
- 采用依据：Q-017 用户原始回答为“A”，接受保留长文本的读取成本、避免人为拆分造成补查和判断的取舍。该选择不代表已经证明任意长结果一次交付都更便宜；实际总 token、覆盖完整性和调用变化仍需验证。
- 重新裁决触发条件：实际容量无法承载、长文本影响任务准确性或总体成本，或实现需要额外补读、重拼和判断时，按具体场景重新裁决，不静默改为默认预览或取消保护。

### C-019｜已有可靠文件范围时优先同次筛选读取（来源：Q-018）

- 当前有效结论：用户选择 A。已知只需某段文件或某类文件，且所需行范围或文件名条件已有可靠依据、足以覆盖任务所需信息时，在同一次调用中直接指定范围或条件，取得所需结果；范围不明确时不猜测，不拆成多次试读，不为筛选增加定位或补读调用。
- 适用前提和边界：这是利用已有参数限制明确无关的内容，不是默认截断分页；所需范围内的结果在容量允许时仍一次完整交付，必要上限与不完整提示保留。尊重用户明确范围和授权目录，不把 pattern 的文件名筛选当成正文内容检索，不由本题新增 list/search 的 limit、游标或检索模型，不改变 C-010 的非界面任务前置边界。
- 采用依据：Q-018 用户原始回答为“A”，接受以已有可靠范围减少无关读取、不增加 AI 定位及补读的取舍。实际覆盖、调用、判断和总 token 尚未对照验证；不把预期减量写成已测量收益。
- 重新裁决触发条件：范围失效或过窄、必要上下文遗漏、参数语义不符，或实际增加定位、补读、误判与重试时，按具体场景重新裁决；不能为坚持筛选强猜范围或默认多次试探。

### C-020｜单纯图片文件压缩暂不纳入本轮（来源：Q-019）

- 当前有效结论：用户选择 A。仅压缩截图文件大小或更换图片编码的优化暂不纳入本轮，保留现有图片编码，优先处理有明确 token 优化路径的项目；不把文件变小直接计作 token 已节省。
- 适用前提和边界：尚无已确认的传输瓶颈或本次任务的实测 token 收益；此项不推翻 C-011 的显式取图、C-012 的已知可靠范围局部优先及 C-013 的不进一步默认降低分辨率，也不排除必要容量保护。它不禁止今后单独评估传输或存储性能，不由本题一并排除内部重复渲染或其他性能问题。
- 采用依据：Q-019 用户原始回答为“A”，接受当前先聚焦模型输入、工具调用和 AI 判断，不扩大为图片编码优化的取舍。文件字节、传输耗时和模型 token 分别看待，未测量的收益不宣称已经实现。
- 重新裁决触发条件：出现明确图片传输、存储或容量瓶颈，获得可复核的实际 token 或重试收益证据，或用户明确要求扩展范围时，再提交裁决；不得静默更换编码或降低清晰度。

### C-021｜未返回文本的内部性能清理暂不纳入本轮（来源：Q-020）

- 当前有效结论：用户选择 A。仅减少程序内部计算、而不减少 AI 接收内容的未返回 UI 文本生成和处理清理，暂不纳入本轮；先聚焦 token、工具调用和 AI 判断，内部性能清理另行处理，不把程序提速直接算作 token 节省。
- 适用前提和边界：尚无证据表明该部分计算造成实际超时或重试。此项不禁止为实现已确认需求所必需的内部重构，不排除有证据减少超时或失败重试的修复；不影响 UI 树排版精简、完整状态加变化及 WaitFor 末次采集复用。
- 采用依据：Q-020 用户原始回答为“A”，接受不为尚无直接或已验证间接 token 收益的内部清理扩大本轮范围。没有否认其耗时或内存优化价值，也未授权修改代码。
- 重新裁决触发条件：出现该计算导致超时或重试的可复核证据、明确性能瓶颈，或用户要求扩展范围时，再按具体场景裁决；不得把它解释为所有内部重构都被禁止。

### C-022｜文字 UI 树优先限定已知可靠范围（来源：Q-021）

- 当前有效结论：用户选择 A。获取文字 UI 树时，目标区域已有可靠依据、足以覆盖任务需要，且不会为缩小范围增加定位或补查调用，则优先在同次调用中限定该范围，交付范围内完整状态与工具内部计算的变化。范围未知、失效或任务需要全局信息时不主动缩小范围。
- 适用前提和边界：只利用已有可靠信息，不强迫先取全桌面状态，不新增范围判断模型；尊重明确范围、必要安全信息、权限、确认和验证。保留范围内逐词坐标、控件完整文本和操作信息，不调低元素上限，不以变化代替完整状态，不自动附图。范围外未观察到的内容不能被报告为不存在或已删除；采集失败或受限须如实标明。WaitFor 仍只复用末次实际采集，不为满足此项再采集一次。
- 待细化事项：范围变化的比较基线可比性、筛选与预算顺序、必要全局元数据和故障提示继续闭环；本题不预先选定新窗口选择器、自动区域识别或存储架构。
- 采用依据：Q-021 用户原始回答为“A”，接受以已有可靠区域减少无关读取、同时不增加定位和补查的取舍。本题独立确认文字观察策略，不是从截图裁剪结论推定；实际覆盖、调用、重试与总 token 收益未测量。
- 重新裁决触发条件：范围失效、遗漏弹窗或必要上下文、比较误报、采集信息不足，或实际增加定位、补查、误操作、重试和总成本时，按具体场景重新裁决；不能为维持短输出强行缩小范围。

### C-023｜修改点局部 token 对照，尽量排除模型智力因素（来源：Q-022 用户修正）

- 当前有效结论：针对修改内容进行修改前后 token 消耗的局部测试，把测试限定在小范围，尽量排除模型智力、临场理解、规划和自由选工具的影响。本轮不要求模型自主完成完整业务任务后比较总消耗，也不是仅验收功能而不测 token。
- 对照边界：优先固定输入参数、文本或 UI 状态样本、初始条件、相关配置及计数口径，直接运行修改前后的对应处理。输出精简用同一输入比较真实输出；减少调用的修改以实现同一局部结果的最小固定操作链比较，不扩展为长任务，也不强行只看某一个变长的回包。各版本执行同样的局部目标、保持必要验证和信息完整性；不得用漏信息、少做工作或提前失败制造节省。
- 计量与证据：文本使用同一明确的 tokenizer、编码及版本计数，记录局部实际输入、输出及相关工具定义的统计边界；同时单列工具调用、额外模型调用的前后数量。内部轮询不算新增 AI 往返，不把一次减少的调用或判断乘以臆造的 token 常数。不要求模型生成自由回答或用另一个模型当裁判；涉及生成、图像或客户端封装而无法由固定文本计数覆盖的部分，采用固定回放或单独标记待测，并区分回放、估算与实际计费用量，不能把图片字节或字符数当 token。
- 结果表达：每个改动报告样本、前后版本、计量口径、修改前值、修改后值、差额和功能检查。接受如实出现的文本增加或零直接减量，另列能够确定的调用减少与变化正确性；不把“少做判断”宣称为已测量的推理 token 节省。局部收益不外推为全任务节省率，也不能把重叠的小流程差额直接相加为整体收益。
- 采用依据：用户原文要求“局部”“小”范围并尽量“派出模型智力的因素”，按语义理解为尽量排除模型智力因素，原文在 Q-022 完整保留。这是对原问题的自定义修正，未接受原 A 的完整任务实测要求，也未接受原 B 的取消 token 实测；C-002 的减少调用与判断目标仍有效，改用受控局部证据评估。
- 适用前提及待细化事项：局部样本与执行路径可固定、可复核，保留所有已确认能力、安全及容量边界。具体计数器、样本规模、图像计量和通过阈值尚未定案；本轮只确认验收要求，未运行测试、未取得前后数值、未授权实施代码或开展有副作用的真实业务测试。该要求不证明任意客户端和模型下的完整任务成功率或成本。
- 重新裁决触发条件：改动效果无法在独立小范围内隔离、生成或外部状态波动主导结果、计量不一致、测试遗漏必要输出或验证，或确实需要扩大测试范围才能判断某项影响时，先说明该项局限并提交裁决，不静默改成大规模自主模型任务测试。

### C-024｜运行时比较最近一次成功交付的可比状态（来源：Q-023 同题澄清 Q-023-A）

- 当前有效结论：用户在澄清后选择 A。变化基线定义为同一会话最近一次成功返回给 AI、与当前观察可比的状态；“上次”指已交付状态，不是内部采集次数，也不是修改前版本的测试输出。正常使用不需要先运行原版建立基线。
- 适用前提和边界：有有效、范围可比的已交付状态时，工具内部计算其与当前状态的净变化。WaitFor 内部轮询、失败采集和未交付状态不更新交付基线；本次成功交付的合格状态才可作为下一次基线。首次没有基线或当前无法可靠比较时，同次交付可用的完整当前状态并明确“无可比基线”或不可比原因，不能伪报“无变化”、跨会话混用、强行比较失效窗口或把未观察、截断缺项当作删除。不补采原版、不另调 diff、不自动截图。
- 采用依据：用户先询问“\"上次\"怎么定义？先用原版跑一遍吗?”，在运行时基线与局部前后测试分开解释后回答 A。原 Q-023 的 A 及同题澄清的 A 一致；澄清中的 C 未被采用，不回退为放弃变化功能。
- 待闭环事项：会话识别、可比性和交付成功的技术可观察判据、并发和传输失败的更新时序仍需验证；当前没有证明已有客户端接收确认协议，不将仅生成响应等同于 AI 确实收到，也不擅自新增一次 AI 确认调用。
- 重新裁决触发条件：无法可靠区分交付与采集、会话或范围混用、基线被轮询覆盖、故障导致错误变化，或方案需要增加 AI 确认和补查时，交用户裁决，不静默退回最近内部采集。
- 领域关联：项目 `CONTEXT.md` 的“交付基线”，关联 Q-023；是已确认含义，具体实现尚未实施，不构成 ADR 或实施授权。

### C-025｜仅维护最近一次交付基线，不引入多版本历史（来源：Q-024）

- 当前有效结论：用户选择 A。由 Windows-MCP 在同一会话内只保留最近一次成功返回给 AI 的状态作为下一次变化比较的候选基线，比较时检查是否可比；不维护多版本历史链，不把历史保存、传回或拼接交给 AI。
- 适用前提和边界：这是保留数量及责任的取舍，不是按分钟设置保留时长。沿用 C-024：内部轮询、失败采集或未交付状态不替换基线；没有基线或不可比时明确说明。当前状态与高层变化仍同次返回，不为查旧版本增加调用，不因只能保存一个基线而省略当前完整信息。本题不选定磁盘持久化、跨重启恢复、存活时间或多区域历史缓存；正常对比所需当前工作状态不等于多版本历史服务。
- 采用依据：Q-024 用户原始回答为“A”，接受最近一次基线足以服务“上次看到有什么变化”、不引入历史管理的取舍；不将减少存储本身宣称为已测量 token 节省。
- 重新裁决触发条件：具体任务确实需要多版本回溯或多范围保留、单基线引发反复不可比或补查，或连接生命周期不能满足需求时，针对影响重新裁决，不擅自引入历史链或让 AI 代管。
- 领域关联：项目 `CONTEXT.md` 的“交付基线”，关联 Q-024；未选定难以逆转的存储架构，不新建 ADR。

### C-026｜变化内容默认采用高层摘要（来源：Q-025）

- 当前有效结论：用户选择 A。完整当前状态之外的变化内容，默认描述窗口变化、元素新增或删除、文本变化等高层变化，不默认逐个字段展开旧值、新值、坐标等全部差异，也不采用只交付节点 ID 或路径索引、让 AI 自行解释变化的路线。
- 适用前提和边界：摘要由工具内部确定性比较产生，不调用额外模型；比较基线及可比性遵守 C-024、C-025。完整状态内的层级、名称、类型、逐词坐标、动作、值和相关状态不被削减；高层摘要不替代完整状态，不取消 C-004 的工具内部对比，不增加 diff 或补查调用。变化描述须清楚对应受影响对象和变化类别，不能把不知道、未观察或被截断报告为无变化或删除。具体摘要字段与格式仍待闭环，不把举例列表当作可任意漏掉影响操作的重要变化的授权。
- 采用依据：Q-025 用户原始回答为“A”，接受默认摘要更简洁、字段级细节不全部重复输出的取舍。提问时“token 较少、适合大多数判断”是预期，不是实测事实；按 C-023 用固定状态检验比较正确性和真实文本 token 差额。
- 重新裁决触发条件：高层描述过于含混、对象无法定位、重要变化被隐藏，导致 AI 重新比较历史、调用 diff、补查或误判时，说明具体缺口并重新裁决，不以短摘要为由转嫁必要比较工作。
- 领域关联：项目 `CONTEXT.md` 的“变化摘要”，关联 Q-025；属于可逆输出粒度取舍，未修改代码、未形成需记录 ADR 的架构决定。

## Grill-Me 期间的对话记录

### Q-001

所属阶段: 1

关联议题：T-01，逐词定位能力边界；用户补充意见同时影响 T-03、T-04、T-10、T-14、T-15、T-16。

当前事实：源码会将部分文档或输入控件中的文本展开为带坐标的单词节点；这些节点可用于单词级定位。证据为本次源码基线下 `tree/service.py` 的 `GetAllWordBoundingBoxes()` 调用与 `_append_word_nodes()`，以及 `tree/views.py` 的语义树输出。实际节省量尚未测量。

完整问题：**第一项：文档中的“逐词定位”能力，要保留到什么程度？**

选项：

- **A. 按需保留（推荐）**：平时只返回控件和文本块，需要精确操作时再获取单词坐标；这类任务可能多一次读取。
- **B. 始终提供**：每次都提供逐词坐标，精确操作更直接，但这部分基本不省 token。
- **C. 不再提供**：取消逐词定位，返回更少，但失去这项能力。

提问时的推荐依据及适用前提：推荐 A，前提是多数操作并不需要精确到单词；这样可以减少无关返回，同时保留必要能力。这是原提问中的建议，已被用户选 B 否决，不作为当前有效结论。

用户原始回答：

> B. 多一次工具调用相当于让AI多做一次判断, 这个成本可比省的这几个token贵多了. 对了, "减少 AI 调用工具的次数”、“减少 AI 做判断的次数" 能够达到一些省 token 的目的.

用户选择：B；同时主动补充减少 AI 工具调用次数和判断次数的优化方向。

采用的答案和结论：保留现有逐词坐标的直接提供方式，不新增按需获取步骤；将 AI 工具调用与判断负担纳入其他优化项的评估。完整有效结论见 C-001、C-002。

采用原因或证据：用户明确裁决新增调用及判断成本比本项少量返回 token 的节省更重要。原 A 的推荐只强调减少默认返回和保留能力，没有充分体现用户对额外往返及决策负担的取舍，因此不再采用。未取得实际计费数据，不把用户的定性判断扩大为任何调用都必然更贵的定量事实。

结论适用前提：针对现有支持逐词输出的路径保留能力；不据此给尚未讨论的预算、分页、批量执行等机制自动作出结论。其他候选要同时评估调用和判断增减，不以减少次数为由取消必要验证。

重新裁决触发条件：用户改变优先级，出现不增加 AI 往返和判断负担的等效替代，或具体场景的实测成本、成功率和验证要求与当前预期冲突时，再由用户裁决。

领域文档关联：本轮未形成需新增术语定义或 ADR 的内容；没有跨目录写入，也未变更实现代码。

记录状态：当前有效。

### Q-002

所属阶段: 1

关联议题：T-02，输入文本重复回显；同时应用 C-002 的调用与判断成本边界。

当前事实：`Type` 的回执直接拼接传入的 `text`；`MultiEdit` 的回执直接拼接每个输入项的文本。它们回显的是输入参数，不是从界面读回的验证结果。证据为重新读取的 `tools/input.py` blob `05c657d2301052417d3a5b9f29fe85013c2d157c` 的 `type_tool` 及 `tools/multi.py` blob `4f4c448fe46f3ee9e5be2869c0f2e06e0217f5d1` 的 `multi_edit_tool`。

完整问题：**第二项：`Type` / `MultiEdit` 输入后，还要把输入全文重复返回吗？**

选项：

- **A. 取消全文回显（推荐）**：同一次调用返回执行状态、目标、字数和必要错误；不新增补查步骤。
- **B. 保留全文回显**：维持现状，接受重复文本的开销。

推荐依据及适用前提：推荐 A 的前提是下一步所需信息仍在这次回执中，不让 AI 再判断“要不要补查”。当前这段全文只是重复调用参数，不是已有的界面读回验证，因此候选改动只减少重复文本，不把执行回执称为保存、提交或界面内容核验成功。保留原有必要错误信息及业务验证边界；不以本题预先批准新增回执模式选择、摘要调用或全文补查机制。

各选项主要影响或代价：A 缩短结果、特别是长文本输入的重复部分，代价是不再从工具回执中取得输入全文；前提是调用方已经拥有这些输入，不需要因此新增读取。B 不改变回执习惯或既有依赖，但保留重复文本的上下文负担。实际全过程 token 收益尚未测量。

用户原始回答：

> A

用户选择：A。

采用的答案和结论：取消 `Type` / `MultiEdit` 的输入全文回显，同一次调用保留执行状态、目标、字数和必要错误；不因精简回执增加补查步骤或“是否补查”的 AI 判断。完整有效结论见 C-003。

采用原因或证据：用户明确选择该题 A；所删除的全文只是已传入参数的重复，不是独立读取到的执行验证。该取舍符合 C-002 对调用与判断负担的要求，不将预计减少的重复文本量当作已实测的全过程节省。

结论适用前提：调用方已经拥有输入文本；回执保留下一步必要信息和真实错误，不省略原有必要验证，不把执行动作完成等同于保存、提交或内容核验成功。此次只确认回执行为，不涉及实施代码或其他工具输出策略。

重新裁决触发条件：客户端依赖全文回显、下一步必要信息不足，或该精简增加补查、判断、重试或误报成功时，重新交用户裁决。

领域文档关联：未形成新的项目术语定义；这是可逆的回执行为需求，不满足新增 ADR 的条件，未产生跨目录写入。

记录状态：当前有效。

### Q-003

所属阶段: 1

关联议题：T-03，交给 AI 的快照是否需要结合历史还原；同时应用 C-002 的判断与补查成本边界，并关联 T-14、T-16。

证据及评估边界：已读取的固定源码基线中，`tools/_snapshot_helpers.py` 的 `build_snapshot_response` 组织本次采集的桌面、窗口及语义树返回，`tools/snapshot.py` 的接口没有差分基线参数。这里评估的是上一轮提出的“只将变化部分交给 AI”候选，而不是现有能力。不会把工具内部做差分、再由确定性程序还原完整结果的实现自动归入选项 B；窗口或区域选择、图像是否附带、格式精简以及既有采集限制仍另行评估。

完整问题：**第三项：快照每次返回完整的当前状态，还是只返回变化？**

选项：

- **A. 保留完整状态（推荐）**：当前观察范围内的信息一次给齐，不要求 AI 对照旧快照拼接；接受重复信息的开销。
- **B. 只返回变化**：减少重复输出，但 AI 需要结合历史状态理解；历史缺失时还可能补取完整快照。

提问时的推荐依据及适用前提：推荐 A，前提是采用用户已明确的取舍，优先避免增加判断和补查，而不是只追求单次回包更短。A 的“完整”指本次观察范围和采集能力内、无需历史差分还原的当前状态，不等于强制采集全部屏幕、取消既有限制或必须同时附图。B 仅在可用基线持续存在且收益足以抵消状态还原负担时可能更合适，尚无实测数据，不能将其提前认定为更省或更贵。这是原提问的推荐，用户随后给出了完整状态加变化内容的组合，不以原二选一限制该修正。

各选项主要影响或代价：A 不引入需要 AI 消化的差分协议，保留重复状态文本的成本；仍可另行评估不丢信息的格式整理或工具内部优化。B 有望减少变化较少时的重复文本，但增加对历史基线的依赖；若选择 B，须继续明确失效恢复、额外调用与判断、节点版本和会话隔离，不在本题自动批准具体实现。

用户原始回答：

> 完整状态+变化内容, 少一次 AI 调用工具执行 diff 的开销

用户选择：自定义修正；完整状态和变化内容一起提供，不是只保留完整状态，也不是只返回变化。

采用的答案和结论：同一次快照调用返回完整当前状态与工具内部直接计算的变化内容；不要求 AI 额外调用 diff 工具、自行逐项比较或基于差分拼接当前状态。变化内容补充完整状态，不替代它。完整有效结论见 C-004。

采用原因或证据：用户明确指出，提供完整状态之外，还可以把对比结果直接给 AI，以减少另行调用工具执行 diff 的开销。因此原问题遗漏了“完整加变化”的组合。采用该修正，而不是把用户归入没有附加变化内容的原 A；工具内部确定性比较也不应变成另一轮模型调用。

结论适用前提：针对本次快照观察范围及采集能力，沿用现有完整状态与 C-001 的能力边界；不以附加变化替代完整信息，不扩大为每次动作后自动新增快照，也不自动批准基线存储、跨会话复用或字段级协议。比较基线、首次或不可比情形及失败可见性待后续细化。节省 diff 调用是目标，实际任务是否曾有该调用及全过程净收益尚未测量。

重新裁决触发条件：对比失真、完整状态与变化冲突、无可比基线误报无变化，或实现反而增加 AI 调用、判断或失败重试时，按具体影响重新裁决，不静默降级成仅差分返回。

领域文档关联：本题确认的是尚未实施的返回行为目标；基线、数据结构及持久化设计未选定，未形成难以逆转的架构决定或需另建术语表的正式定义。本轮只更新本需求草稿。

记录状态：当前有效。

### Q-004

所属阶段: 1

关联议题：T-04，大输出的默认交付方式；同时应用 C-002，关联 T-11、T-14。

评估边界：这里讨论文件、日志或命令等大段输出是否仅为省 token 就默认截断、要求 AI 分次读取。此前对 `PowerShell`、`FileSystem`、`Clipboard` 的静态分析与定位仍保留在 T-04；各工具现有上限并不相同，本题不把某个默认分页机制说成已实现，也不虚构模型或客户端的具体容量。一次返回本次请求的完整结果，不等于无视用户已给的筛选或读取范围、无条件读取整台机器的文件，或取消必要容量保护。

完整问题：**第四项：文件、日志或命令输出很长时，是否默认截断、让 AI 分次读取？**

选项：

- **A. 不为省 token 强制分页（推荐）**：在容量允许时，一次返回本次请求的完整结果；超出必要安全上限时明确提示不完整。
- **B. 默认截断分页**：每次只给一部分，单次返回更短，但可能增加补读调用和判断。

推荐依据及适用前提：推荐 A，延续用户“减少调用和判断”的优先级，但不取消防止超大结果撑爆上下文的保护。适用前提是本次请求结果能在相关容量和安全边界内合理承载；既定读取范围、显式筛选和必要错误仍应被尊重。具体阈值、超限后如何保留内容和继续读取，尚待后续取舍，不在本题提前写成结论。

各选项主要影响或代价：A 避免把本可一次交付的结果拆成多次 AI 往返，代价是正常情况下可能保留较长正文；超限时仍需要如实暴露不完整，不能承诺无限输出。B 控制单次输出量，但需要评估后续补读和“是否补读”的判断成本，只有任务所需部分被覆盖且总体收益合理时才可能划算。两项均不允许静默截断后冒充完整结果。

用户原始回答：

> A

用户选择：A。

采用的答案和结论：不为省 token 强制分页，在容量允许时一次交付本次请求的完整结果；保留必要安全上限，超限时明确提示不完整。完整有效结论见 C-005。

采用原因或证据：用户明确选择 Q-004 的 A，接受避免额外补读和判断、同时保留容量保护的取舍。它不授权无限输出，也未取消已有的显式筛选或范围参数。

结论适用前提：本次请求结果处于可合理承载的容量范围；超限阈值、超限后保留和取回完整内容的方式，以及各工具具体差异仍待细化。

重新裁决触发条件：客户端承载能力、成本或成功率出现与当前取舍不符的实际证据，或方案需要对正常结果重新引入强制分页、取消保护时，提交用户裁决。

领域文档关联：本题未形成新的业务术语定义或难以逆转的架构选择；本轮仅维护本需求草稿，未修改实现代码。

记录状态：当前有效。

### Q-005

所属阶段: 1

关联议题：T-05，网页抓取默认额外模型摘要；同时应用 C-002，关联 T-14、T-16。

当前事实：`Scrape` 的 `use_sampling` 默认 True；存在 `ctx` 时尝试以抓取内容调用 `ctx.sample`，让模型整理内容后返回，异常时回退抓取内容。`query` 仅在这一摘要调用中起定向作用，`max_tokens=2048` 是该调用的输出上限。证据为本轮完整重读的 `tools/scrape.py`，blob `ad11580fc8f8279cc47475a33274e9cc61aa8175`。这描述的是代码默认尝试的行为，不能据此断言用户客户端每次都实际执行了摘要调用，也不能预设关闭后全过程 token 必然下降。

完整问题：**第五项：网页抓取后，是否保留默认的额外 AI 摘要？**

选项：

- **A. 默认不做额外 AI 摘要（推荐）**：由程序清理网页杂项后直接返回内容，省去这次模型调用；返回可能更长。
- **B. 保留默认 AI 摘要**：先让模型整理再返回，结果可能更精简，但保留额外模型的读取和生成开销。

推荐依据及适用前提：推荐 A，前提是程序清理不丢失任务所需内容，也不引入补读。它优先减少默认链路中的额外模型调用，而不是只追求更短的工具回包；主模型读取更长内容的成本也应计入全过程比较，不宣称单凭少一次摘要就必然降低总成本。

各选项主要影响或代价：A 不再默认让另一次模型处理同一份内容，代价是主模型可能读取更多文本，且程序清理不能替代所有语义提取。B 保留模型整理和定向提取能力，但摘要模型仍需读取抓取内容并生成结果，摘要遗漏还可能导致后续补查。具体净收益需要结合实际客户端和任务验证。

评估边界及后续依赖：本题只裁决默认摘要链路，不把 A 扩张为禁止用户显式请求摘要，也不预先决定删除 `use_sampling` 参数或引入新的模式选择。程序清理是候选行为，不冒充当前已经实现的完整正文清理；具体保留与去除规则仍需核对。当前 `query` 的定向提取依赖摘要路径，若选择 A，必须继续明确如何保持或调整该语义，不能静默忽略 query、伪装已完成定向提取，或让 AI 因此反复补查。容量不足时仍受 C-005 的必要保护和不完整提示约束。

用户原始回答：

> A

用户选择：A。

采用的答案和结论：默认不做额外 AI 摘要，由程序清理网页杂项后直接返回内容；不丢失任务所需信息、不引入补读。完整有效结论见 C-006。

采用原因或证据：用户明确选择 Q-005 的 A，接受省去默认摘要模型处理、主模型可能读取较长正文的取舍；不能据此虚报已实现或已测量的 token 节省。

结论适用前提：只改变默认摘要链路；`query`、显式摘要、清理规则及回退兼容仍待细化，不能静默忽略参数。必要容量保护继续适用。

重新裁决触发条件：清理丢失内容、兼容冲突、补读和判断增加，或实际全过程成本与成功率不符目标时，按具体场景重新裁决。

领域文档关联：未形成新业务术语定义或难以逆转的架构决策；本轮只更新本需求草稿，未修改实现代码。

记录状态：当前有效。

### Q-006

所属阶段: 1

关联议题：T-06，UI 树的排版与操作信息；同时遵守 C-001、C-002 和 C-004。

当前事实：`tree/views.py` 的 `_format_semantic_node`、`_render_semantic_node` 及元数据格式化函数返回控件名称和类型、坐标、动作提示、相关状态，并以连接线及缩进表达层级。证据为本轮重读的该文件第 20–180 行，blob `8a49958f8a450ebfcab72ce39a36c4d6376b8927`。仅减少字符不一定减少目标模型的 token 或理解成本，尚未进行对照测量。

完整问题：**第六项：UI 树要不要只精简排版，不删操作信息？**

选项：

- **A. 只精简排版（推荐）**：减少多余空格和装饰符，保留层级、名称、坐标、动作提示及状态；不增加工具调用。
- **B. 保持原格式**：不做这项优化，保留现有排版开销。

推荐依据及适用前提：推荐 A 的前提是 AI 理解起来不更费劲；不能为了省几个 token，让 AI 自己猜动作或层级。层级连接符中实际承载结构的信息不属于可随意删除的装饰，精简后仍须直接清楚表达。保留逐词定位、完整状态加变化内容以及控件类型、值、焦点、勾选、展开、范围等已有操作信息，不以删减信息或转成额外按需读取来压缩。

各选项主要影响或代价：A 仅针对排版尝试减量，预期收益可能有限，需要验证实际 token、理解准确性和现有消费者兼容；不得增加新的模式选择、补查或解码调用。B 保持表达方式，不引入格式兼容变化，但不取得这一项可能的节省。具体序列化格式、编号和阈值未在本题定案，也不因原候选提过 JSON 就默认采用它。

用户原始回答：

> A.

用户选择：A。

采用的答案和结论：只精简冗余排版，保留完整的层级及操作信息；不增加 AI 理解负担和工具调用。完整有效结论见 C-007。

采用原因或证据：用户明确接受 Q-006 的 A 及其不删信息、不增加理解负担的前提；不将这一选择扩张为删除动作提示或让 AI 自行还原结构。

结论适用前提：层级、名称、类型、坐标、动作提示、值和相关状态仍清楚可用；已有逐词定位及完整状态加变化内容不被削减。具体格式未定，实际收益与兼容性待验证。

重新裁决触发条件：发生结构歧义、信息丢失、消费者兼容问题，或增加判断、补查和重试时重新裁决。

领域文档关联：本题是可逆的排版行为取舍，未形成新的项目术语定义或难以逆转的架构决定；本轮只更新本需求草稿。

记录状态：当前有效。

### Q-007

所属阶段: 1

关联议题：T-07 的工具说明精简；同时遵守 C-002。工具集合和调用前置要求尚未裁决。

证据及评估边界：既有源码证据中的工具描述包含用途、参数用法和关键词说明，具体定位见 T-07 及已读取的 `tools/snapshot.py`、`tools/shell.py`、`tools/scrape.py`。本题只评估不提供新信息的重复文案，不改变参数类型、默认值、兼容处理或工具能力，也不将必要说明拆到需要另行发现或查询的文档中；实际 token 和调用成功率仍待验证。

完整问题：**第七项：工具说明要不要去掉重复内容，但保留必要用法？**

选项：

- **A. 精简重复说明（推荐）**：保留用途、参数约束、适用条件和安全提示；不让 AI 额外查说明或猜参数。
- **B. 保持原说明**：不改现有表述，保留重复说明的开销。

推荐依据及适用前提：推荐 A，只去掉不提供新信息的重复，不以增加误用、判断或重试为代价。工具选用和参数填写所需信息仍直接可得；不能机械删除帮助模型正确选用工具的关键词或必要示例。

各选项主要影响或代价：A 有望缩短模型接收的说明，但需验证用法仍清楚且调用成功率不下降；B 不引入文案变化，保留原说明长度。具体删改文本、客户端的工具注入与缓存收益未定，不承诺按源文件字数同比省 token。隐藏工具、按需启用和是否必须先调用 Snapshot 是不同取舍，不由本题一并批准。

用户原始回答：

> A

用户选择：A。

采用的答案和结论：精简不提供新信息的重复说明，保留用途、参数约束、适用条件和安全提示；不让 AI 额外查说明或猜参数，不增加误用、判断或重试。完整有效结论见 C-008。

采用原因或证据：用户明确选择 Q-007 的 A；该取舍仅减少重复文案，不将必要用法迁移到另一次读取中，也不取消现有参数兼容或工具能力。实际 token 节省及调用准确性仍待验证。

结论适用前提：工具选用和参数填写所需信息直接可得；必要关键词、示例和安全提示保留。工具集合、动态启用和调用前置要求仍另行评估，不因这一回答一并定案。

重新裁决触发条件：用途或参数含混、必要说明缺失、误用与重试增加，或客户端兼容和安全提示出现问题时重新裁决。

领域文档关联：本题是可逆的说明文案取舍，未形成新的项目术语定义或难以逆转的架构决定；本轮只更新本需求草稿，未修改实现代码。

记录状态：当前有效。

### Q-008

所属阶段: 1

关联议题：T-07 的工具集合提供方式；同时应用 C-002 和 C-008，关联 T-14、T-16。

当前事实及评估边界：本轮完整读取的 `tools/__init__.py`，blob `b4b5c95c08c01292594b5a823358d2ce26068794`，由 `register_all` 遍历 `_MODULES` 注册工具模块。这里裁决项目是否新增面向任务的工具缩减或加载机制，不断言客户端实际每轮暴露、发送或计费全部定义。所有选项均遵守既有权限与安全配置，不为省 token 自动扩大权限或绕过禁用项；用户已在客户端手工禁用的工具不因“全部提供”而重新开启。

完整问题：**第八项：工具要一次全部提供，还是按任务缩减？**

选项：

- **A. 默认全部提供（推荐）**：AI 直接选用，不新增查找、启用或切换步骤；接受工具说明的开销。
- **B. 预先固定精简**：由你配置只启用部分工具，不增加 AI 加载调用；任务超出范围时需调整配置。
- **C. 运行时按需加载**：先提供少量工具，缺什么再加载；说明可能更少，但增加选择和加载步骤。

推荐依据及适用前提：推荐 A 的前提是任务类型不固定，优先避免为了隐藏工具而增加选择和调用。尚未确认可以长期安全排除的具体工具集合，不擅自替用户固定精简范围。若任务范围稳定且用户愿意预先维护集合，B 能避免运行时加载判断，不与 C 混为一谈；若实际客户端的动态加载收益足以抵消新增步骤，可进一步评估 C。

各选项主要影响或代价：A 保持已授权工具直接可用，承担必要工具定义的上下文成本；B 减少提供的工具定义，代价是配置维护及需求超出集合时的恢复；C 可按变化的任务提供工具，代价是新增发现、判断、加载及相关兼容机制。具体节省取决于客户端注入和缓存行为，不能仅按服务端注册数量推断。C 是候选新增策略，不冒充当前已实现；本题也不改动工具内部的延迟初始化或内存加载方式。

#### 同题补充澄清及工具清单

用户在选择前的原始请求：

> 把所有工具列出来我看一下

已向用户展示的清单如下；代码核对基线为 `7aa5493fc8e33fc7e0da64ff4a0282d5094197b5`。它是现有能力说明，不是本次已实施的优化，也不表示用户客户端实际可用权限。上轮未写回的内容在本轮补记，不另建问题编号。

| 分类 | 工具 | 已展示用途 |
| --- | --- | --- |
| 观察与窗口 | Snapshot | 获取桌面、窗口、UI 元素及坐标，可同时附带截图。 |
| 观察与窗口 | Screenshot | 快速截图，跳过 UI 树提取。 |
| 观察与窗口 | DisplayInventory | 查看显示器布局、分辨率、缩放比例等。 |
| 观察与窗口 | App | 启动程序、切换窗口、调整窗口位置和大小。 |
| 鼠标键盘与批量 | Click | 单击、双击、右键等，通过坐标或元素编号定位。 |
| 鼠标键盘与批量 | Type | 向一个位置输入文本，可清空原内容、输入后按回车。 |
| 鼠标键盘与批量 | Scroll | 垂直或水平滚动。 |
| 鼠标键盘与批量 | Move | 移动鼠标，也支持拖拽。 |
| 鼠标键盘与批量 | Shortcut | 执行键盘快捷键，例如复制、粘贴。 |
| 鼠标键盘与批量 | MultiSelect | 一次选择多个项目，或连续点击多个位置。 |
| 鼠标键盘与批量 | MultiEdit | 一次向多个输入框填写文本。 |
| 等待 | Wait | 固定等待指定秒数。 |
| 等待 | WaitFor | 等待指定文字、窗口或控件条件满足，由工具内部反复检查。 |
| 文件网页与系统 | FileSystem | 读写、复制、移动、删除文件；列目录、查找文件、查看文件信息。 |
| 文件网页与系统 | Clipboard | 读取或设置剪贴板文本。 |
| 文件网页与系统 | Scrape | 抓取网页内容，支持 HTTP 请求或当前浏览器页面的 DOM 路径。 |
| 文件网页与系统 | PowerShell | 执行 PowerShell 命令或脚本，进行系统自动化。 |
| 文件网页与系统 | Process | 查看、筛选运行中的进程，按名称或进程编号终止进程。 |
| 文件网页与系统 | Registry | 读取、设置、删除注册表项或值，列出子项。 |
| 文件网页与系统 | Notification | 发送 Windows 桌面通知。 |

共 20 个。来源为该基线的 `src/windows_mcp/tools/__init__.py` 注册模块及各模块内工具定义；其中输入、等待、批量、快照、文件、剪贴板、抓取和命令工具的来源已列于“源码定位”，窗口及系统补充定义位于同目录的 `app.py`、`display.py`、`process.py`、`registry.py`、`notification.py`。其中 `_MODULES` 的模块数不等于工具数，一个模块可注册多个工具。

#### 用户裁决

用户原始回答：

> Q8: A

用户选择：A。

采用的答案和结论：默认全部提供已授权工具，AI 直接选用；不新增查找、启用、切换或运行时按需加载步骤，接受必要工具说明的开销。不采用本次候选的固定精简或动态加载方案。完整有效结论见 C-009。

采用原因或证据：用户查看现有 20 个工具的用途后明确选择 Q-008 的 A，与减少额外调用和判断的目标一致。用户未给出需要排除的具体工具，不据推荐前提反推其任务类型一定不固定，也不把接受说明开销当成已测量的净节省。

结论适用前提：保留既有权限、安全设置和客户端禁用项；不自动重新启用被禁用工具，不把全部提供理解为全部执行。C-008 的必要说明和参数兼容约束继续适用；本项不强制客户端如何注入、缓存工具定义，不涉及代码实施。

重新裁决触发条件：用户改变工具范围或权限，或具体客户端承载能力、准确性及全过程成本与选择冲突时，再由用户裁决，不自行缩减或改为动态加载。

领域文档关联：本题保留现有工具可用方式，未形成新的领域术语或难以逆转的架构决定；本轮只更新本需求草稿，并补齐同题澄清记录。

记录状态：当前有效。

### Q-009

所属阶段: 1

关联议题：T-07 的 Snapshot 调用前置要求；同时应用 C-002、C-008 和 C-009，关联 T-16。

当前事实与评估边界：重新读取的 `tools/snapshot.py` 第 1–52 行，blob `5ac15b874ebed446d33921981ce9d50800c823f0`，描述中包含行动前先调用 Snapshot 的要求。本题评估是否收窄该说明的适用范围，不声称所有非界面工具存在强制先调用 Snapshot 的运行时门禁，也不假设用户每个任务实际都有一次多余快照。

完整问题：**第九项：不依赖界面的任务，还要先获取桌面快照吗？**

选项：

- **A. 不需要（推荐）**：文件路径或命令已明确、且不依赖界面时直接执行；需要界面定位时仍先观察。
- **B. 仍然先获取快照**：所有任务统一先调用 Snapshot，接受与当前任务可能无关的调用和返回开销。

推荐依据及适用前提：推荐 A，只取消与任务无关的前置调用，不省略必要的权限检查和结果验证。适用前提是目标、参数及执行条件已明确，执行不依赖当前窗口、焦点、坐标或视觉状态。把这类边界直接说明清楚，不新增“检查是否需要快照”的工具调用、额外模型裁判或可选模式；不能仅因使用 PowerShell 就推定脚本一定不依赖界面。

各选项主要影响或代价：A 在符合前提的任务中可省去先观察桌面的往返，但需要说明清楚非界面任务与依赖 UI 状态任务的边界；B 保持统一入口习惯，却对不依赖桌面的任务增加观察步骤。实际省去的调用量和 token 待任务对照验证。本题不改变 C-004 已要求的快照内容、不授权盲点坐标或使用失效标签，也不决定每个 UI 动作后的观察频率。

用户原始回答：

> A

用户选择：A。

采用的答案和结论：不依赖界面且目标、参数明确的任务不必先获取桌面快照；需要界面定位时仍先观察，权限检查和必要结果验证保留。完整有效结论见 C-010。

采用原因或证据：用户明确选择 Q-009 的 A，接受取消无关前置调用而非取消必要观察和验证。现有证据针对说明文字，不能把预期省去的调用写成已测量收益。

结论适用前提：任务不依赖当前窗口、焦点、坐标或视觉状态；不新增“是否需要快照”的工具或模型判断步骤，不改变已确定的快照内容及现有权限边界。

重新裁决触发条件：实际依赖 UI 状态、出现误操作或重试，或需要新增选择、裁判调用时，按场景重新裁决。

领域文档关联：本题是可逆的前置行为取舍，未形成新领域术语或难以逆转的架构决定；本轮只维护原需求草稿，未修改代码。

记录状态：当前有效。

### Q-010

所属阶段: 1

关联议题：T-08，界面观察的截图提供方式；同时遵守 C-001、C-002、C-004、C-007 和 C-010，关联 T-09、T-14、T-16。

当前事实：本轮完整重读 `tools/snapshot.py`，blob `5ac15b874ebed446d33921981ce9d50800c823f0`。Snapshot 默认不带图，可通过 `use_vision=True` 同次附图；Screenshot 固定取图并跳过 UI 树。本题的工具内部自动补图是候选新增行为，不是当前参数已经实现的策略。

完整问题：**第十项：观察界面时，截图怎么提供？**

选项：

- **A. 工具自动兜底（推荐）**：通常只返回 UI 树；提取失败或为空时，同一次调用附图。已明确需要看图时直接带图。
- **B. 每次都带图**：UI 树与截图一起返回，减少事后补图，但每次都有图像开销。
- **C. 由 AI 按需选图**：维持现有方式，文字信息不足时可能再调用截图。

提问时的推荐依据及适用前提：推荐 A，适合主要可由 UI 树描述的控件操作；把能由程序确定的提取失败或为空情形放在工具内部补图，避免明显失败后再让 AI 单独取图。不调用额外模型判断是否补图；已明确需要查看图表、图像等内容时直接在本次请求附图，不强迫先走一轮纯文本。非空 UI 树仍可能缺少视觉信息，因此 A 不承诺识别所有视觉需求或彻底消除补图；B 对这类不确定性更保守，但正常观察也承担图像输入。此为原推荐，已被用户明确要求显式取图的修正否决，不作为当前有效结论。

各选项主要影响或代价：A 通常避免不必要的图片，在可检测失败时减少一次 AI 往返，代价是新增程序兜底及错误处理，且不能仅据树非空认定信息完整。B 尽量一次给齐两类可采集信息，代价是每次传图及图像处理开销；图和树都可能采集失败，不能宣称必然完整。C 保持现有控制和兼容方式，但仍由 AI 决定是否附图，遇到缺失可能另行调用。各项净收益均须结合实际客户端视觉支持和任务验证。

提问时的评估边界及后续依赖：本题只决定拟采用的提供策略，不删除独立 Screenshot、不取消截图授权和调用者明确的禁图要求，也不把 `use_ui_tree=False` 的主动跳过当成采集失败。失败/空树判据、部分采集失败、同次捕获的时间与坐标一致性、截图仍失败的明确回执、显式参数兼容及基线处理待后续细化。仍保留已有逐词信息和完整状态加变化内容；UI 树失败时如实标明不可用，不能用截图伪装成成功取得完整树；不扩张为非界面任务或每个动作后强制截图。用户本轮已否定自动补图，相关自动兜底机制不再作为已选路线的待实现内容。

用户原始回答：

> 要获取截图时由 AI 显式调用, 因为 AI 看图比较费 token, 所以非必要不提供

用户选择：自定义明确截图门槛，与原 C 的显式选择方向一致；不采纳原 A 的程序自动补图或原 B 的每次带图。

采用的答案和结论：截图仅在 AI 显式请求时提供，非必要不提供；UI 树提取失败、为空或不完整也不自动附图，改为如实返回文字状态和必要错误。完整有效结论见 C-011。

采用原因或证据：用户明确将避免非必要图像处理开销置于自动补图省一次调用之上。这是截图场景的具体取舍，不与 C-002 冲突，也不能用减少 AI 调用的总方向推翻它。原 A 对自动补图的推荐不再采用；实际成本没有测量，不记录泛化的定量断言。

结论适用前提：默认文字观察和已确认的完整状态加变化内容保留；必要截图可直接显式请求，不强迫进行纯文本调用，不删除现有显式取图能力。失败信息必须可见，不伪装为完整树成功。截图范围、分辨率及具体接口边界尚待细化。

重新裁决触发条件：用户改变取图门槛，或具体场景出现不可接受的多次取图、信息不足、成本或成功率冲突时重新裁决；不得自行恢复自动补图。

领域文档关联：本题是截图触发行为的可逆取舍，不新定义领域术语或选定难以逆转的架构；本轮仅更新原需求草稿，未修改实现代码。

记录状态：当前有效。

### Q-011

所属阶段: 1

关联议题：T-09 的截图范围，关联 T-08、T-14；遵守 C-002、C-004 和 C-011。

证据及评估边界：已读取的 `tools/snapshot.py`，blob `5ac15b874ebed446d33921981ce9d50800c823f0`，Snapshot 与 Screenshot 均支持 `region=[left,top,right,bottom]`，并支持 display 选择。此题评估如何使用已有范围能力，不冒充新增了裁剪功能，也不按面积比例承诺图像 token 收益；仅在 AI 已明确决定取图后讨论范围，不重新裁决是否自动附图。

完整问题：**第十一项：已决定截图、且目标区域已知时，要不要优先只截该区域？**

选项：

- **A. 优先局部截图（推荐）**：同一次请求直接指定已知区域；范围未知或需要全局信息时取完整画面，不为裁剪额外查一次。
- **B. 默认完整画面**：保留周边信息，降低裁剪遗漏的风险，但可能带入更多无关图像。

推荐依据及适用前提：推荐 A，前提是已有可靠范围且足以覆盖任务所需内容；只利用已有信息，不让 AI 为了裁剪再取坐标或重截。范围失效、任务需要周边信息时不套用局部优先；调用者已明确指定的范围仍应尊重。

各选项主要影响或代价：A 可减少无关画面，代价是错误或过窄范围可能丢失上下文，因此不猜测范围、不额外增加定位调用；B 保留较多上下文，但承担更多无关画面及潜在缩小后细节不足的成本。实际图像 token、可读性、坐标正确性和重截次数需验证，不承诺任意模型下的固定节省比例。

后续边界：本题不改变截图显式请求门槛，不自动收窄所有 UI 树观察，不修改分辨率默认值或上限，不要求取消必要错误检查，也不预先批准窗口自动识别、智能裁剪模型或分块多图机制。

用户原始回答：

> A

用户选择：A。

采用的答案和结论：AI 已决定取图且已有可靠、足够的范围时优先局部截图，同一次请求直接指定范围；未知范围、范围失效或需要全局信息时取完整画面，不为裁剪额外定位或重截。完整有效结论见 C-012。

采用原因或证据：用户明确选择 Q-011 的 A，接受以已有可靠信息减少无关画面、不增加调用的前提。没有授权自动截图或通过另一轮调用、模型识别来获取裁剪范围；收益仍须验证。

结论适用前提：遵守 C-011 的显式请求门槛，已有范围能覆盖任务所需信息且仍有效，调用者的明确范围和必要校验保留。本题不改变分辨率、UI 树采集范围或元素上限。

重新裁决触发条件：范围不可靠、遗漏必要上下文、坐标错误，或出现额外定位、重截和判断负担时，按具体场景重新裁决，不强行为裁剪增加步骤。

领域文档关联：本题仅确认已有范围能力的使用取舍，未定义新业务术语或难以逆转的架构；本轮只维护原需求草稿，未修改代码。

记录状态：当前有效。

### Q-012

所属阶段: 1

关联议题：T-09 的截图分辨率；关联 T-14，遵守 C-002、C-011 和 C-012。

当前事实与评估边界：本轮读取的 `tools/_snapshot_helpers.py` 第 19–33 行，blob `8fb77fad5caf6fcbf4599f33f6b96ed7a5de6791`，设有 1920×1080 的图像尺寸上限常量，截图缩放环境变量未设置时为 1.0。已读取的 `desktop/service.py` 会结合尺寸上限和缩放设置调整图片，因此“沿用现有设置”不等于取消已有缩放或始终返回原始分辨率。本题讨论是否进一步默认缩小已显式请求的截图，不重新讨论截图门槛或已确定的局部优先策略。

完整问题：**第十二项：截图要不要默认进一步降低分辨率？**

选项：

- **A. 不额外降低（推荐）**：沿用现有尺寸上限和缩放设置，不再默认进一步缩小图片。
- **B. 默认进一步缩小**：尝试降低图像开销，但小字和细节可能更难辨认，需要重新截图。

推荐依据及适用前提：推荐 A，前提是现有截图足够清楚；避免省了图像开销，却增加重截和判断。此前已确认仅显式取图、已知可靠范围时优先局部截图，本题不把这些选择扩张为强制再降低清晰度。现有设置无法满足任务可读性时应如实记录问题，不假定保持现状必然足够。

各选项主要影响或代价：A 保持当前尺寸策略和用户已有配置，不取得额外默认降分辨率的潜在节省，也不新增该项造成的细节损失；B 可能减少图像输入，但须验证实际 token、文字和细节辨认、坐标使用及重截次数，具体比例尚未选定。两项均不允许用文件字节或像素下降比例冒充已测量的 token 收益，不新增另一轮模型判断截图是否清楚，也不自动发送多档图片。

后续边界：保留已有显式截图入口和必要容量保护；本题不选择 PNG/JPEG 文件编码，不决定取消现有图像尺寸上限，不影响 UI 树元素预算。原建议中的 0.75 不是用户已批准的默认值。

用户原始回答：

> A

用户选择：A。

采用的答案和结论：沿用现有尺寸上限和缩放设置，不默认进一步缩小截图，保留用户已有配置。完整有效结论见 C-013。

采用原因或证据：用户接受原 A，优先避免额外降清晰度带来的重截和判断；没有选择新的缩放比例或取消已有上限，未测量实际净收益。

结论适用前提：现有截图满足任务可读性；显式取图和已知可靠范围局部优先不变。不把沿用现状理解为强制原图、不受容量保护或取消用户配置。

重新裁决触发条件：现有设置不能满足可读性、坐标或容量要求，或实际重截与整体成本出现冲突时，针对具体场景重新裁决。

领域文档关联：此项保留已有尺寸策略，未形成新项目术语或难以逆转的架构决定；本轮只维护原需求草稿，未修改代码。

记录状态：当前有效。

### Q-013

所属阶段: 1

关联议题：T-10 的等待策略，关联 T-14、T-16，遵守 C-002、C-009 和 C-011。

当前事实：重新读取的 `tools/input.py` 第 420–560 行，blob `05c657d2301052417d3a5b9f29fe85013c2d157c`，已有 `WaitFor` 在工具内循环采集无图 UI 树、检查指定条件，满足后返回文字回执，超时抛出错误；`Wait` 仅等待固定时长。本题评估使用策略，不声称已新增能力或已修复底层超时、条件判断的全部问题。

完整问题：**第十三项：等待界面就绪时，要不要把反复检查交给工具？**

选项：

- **A. 工具内部等待（推荐）**：条件明确时，一次调用 `WaitFor`，由工具检查，条件满足或超时后返回。
- **B. AI 分步等待**：AI 反复调用等待和快照，再判断是否就绪；保留这些调用和判断开销。

推荐依据及适用前提：推荐 A，前提是等待条件能够被工具可靠检测；减少 AI 反复判断“好了没有”。超时明确报错，不无限等待，也不自动截图。条件不明确或不能由现有检测可靠表达的场景不强行套用，不把单个条件命中当作全部业务验证通过。

各选项主要影响或代价：A 把重复检查留在程序内部，仍有本地采集、等待耗时和条件误判风险；B 便于 AI 根据每次状态灵活判断，但产生重复的模型往返。正常等待过程中不使用额外模型裁判。两项的净成本和成功率仍需实际任务对照。

后续边界：保留既有 `Wait` 和 `WaitFor`，不违背全部提供工具的选择。是否在等待结束时同次返回后续操作需要的状态、与快照变化基线如何衔接、超时和采集异常怎样恢复，仍需后续细化；不在本题批准强制新增快照或自动重试有副作用的动作。UI 树元素上限、长值和批量交互仍保留为未决事项，不因优先讨论等待而视为已通过。

用户原始回答：

> A

用户选择：A。

采用的答案和结论：等待条件明确且可可靠检测时，优先一次调用 WaitFor，在工具内部反复检查，满足或超时后返回；超时明确报错，不无限等待、不自动截图。完整有效结论见 C-014。

采用原因或证据：用户明确接受 Q-013 的 A，减少 AI 反复等待、取快照及判断“好了没有”的往返。已有源码支持这一路径，不把本次选择当作新增功能、运行验证完成或实际 token 节省证明。

结论适用前提：条件可可靠表达和检测，不把条件命中当作全部业务验证通过；保留 Wait、WaitFor、必要权限与验证及显式取图门槛。等待后的信息交付、变化基线衔接及异常恢复继续细化，不自动重试有副作用的动作。

重新裁决触发条件：检测误判、采集阻塞、超时不及时、错误状态不清楚，或实际增加判断、调用和重试时，按场景重新裁决。

领域文档关联：本题是已有等待能力的使用取舍，未形成新领域术语或难以逆转的架构决定；本轮只维护原需求草稿，未修改代码。

记录状态：当前有效。

### Q-014

所属阶段: 1

关联议题：T-10 的批量交互，关联 T-14、T-16；遵守 C-002、C-003、C-009 和 C-011。

当前事实：本轮完整读取的 `tools/multi.py`，blob `4f4c448fe46f3ee9e5be2869c0f2e06e0217f5d1`，已有 MultiEdit 用于同次填写多个输入位置，MultiSelect 用于同次选择或点击多个位置，支持坐标或标签列表。此题评估使用已有工具的优先级，不把它们说成任意动作批处理、自动事务回滚或已能检测所有中途变化的通用执行器。

完整问题：**第十四项：多个输入框或选择项已经明确时，要不要优先一次批量操作？**

选项：

- **A. 优先批量（推荐）**：用 `MultiEdit` / `MultiSelect` 一次处理多个已知目标，减少逐项调用。
- **B. 仍然逐项操作**：每次只处理一个目标，便于中途检查，但保留更多调用和判断。

推荐依据及适用前提：推荐 A，前提是目标已知、布局稳定，且中间不需要新的判断或确认；需要根据上一项结果决定下一项、页面跳转、弹窗或布局变化时仍分步处理，不为了少调用而盲目合并。只利用已有可靠目标和输入，不另调模型判断是否值得批量。

各选项主要影响或代价：A 可以减少同类、确定性步骤之间的 AI 往返，代价是对目标有效性及中途变化更敏感，必须保留必要结果验证和真实失败信息；B 保持细粒度控制，在确需逐项观察时合理，但对已确定的稳定步骤也保留往返开销。实际批量成功率、调用和重试变化、全过程 token 仍需对照，不将工具动作数量减少误写为底层点击或输入次数消失。

后续边界：本题不合并或删除现有工具，不批准跨页面的任意动作脚本或自动提交，不越过权限与必要确认，不默认截图；MultiEdit 回执继续遵守 C-003 的取消全文回显。部分完成、失败停止和恢复的具体表达仍待细化，不假定已有事务回滚或所有项均已成功，也不默认失败后整批重复执行。等待结束后的状态交付、元素上限、长值及其他未决议题继续保留。

用户原始回答：

> A

用户选择：A。

采用的答案和结论：已知目标、稳定布局且中途无需新判断或确认时优先一次批量操作；跳转、弹窗、布局变化或结果依赖时仍分步。完整有效结论见 C-015。

采用原因或证据：用户明确选择 Q-014 的 A；仅确认减少稳定、同类操作之间的 AI 往返，不等于批准任意动作编排或已完成可靠性验证。

结论适用前提：目标仍有效，权限、必要确认与结果验证保留；不自动截图，不回显输入全文，不假定整批成功、回滚或失败后整批重试。部分完成和恢复机制仍待细化。

重新裁决触发条件：目标或布局不可靠、中途需要新判断或确认、部分失败难以辨明，或增加误操作、重试和总成本时，按场景重新裁决。

领域文档关联：这是已有批量能力的使用取舍，没有新术语定义或难以逆转的架构决定；本轮仅更新原需求草稿。

记录状态：当前有效。

### Q-015

所属阶段: 1

关联议题：T-10 的等待结束后信息交付；关联 T-03、T-14、T-16，遵守 C-002、C-004、C-005、C-011 和 C-014。

证据及评估边界：既有已读 `tools/input.py` 第 420–560 行，blob `05c657d2301052417d3a5b9f29fe85013c2d157c`，WaitFor 已在内部取得 desktop_state，但当前满足条件时只返回文字回执，超时抛错。本题评估把已采集状态一并返回的新增行为，不把它说成已有能力，也不把等待命中等同于全部业务成功。

完整问题：**第十五项：WaitFor 等待结束后，要不要同次返回完整界面状态＋变化内容？**

选项：

- **A. 同次返回（推荐）**：复用等待时最后一次采集，连同等待结果一起返回；避免 AI 再调用 Snapshot 获取同一份状态。
- **B. 只返回等待结果**：回执更短，需要界面信息时再调用 Snapshot。

推荐依据及适用前提：推荐 A，前提是不额外采集、不自动截图；采集失败或不完整时如实标明。让已采到的信息直接用于下一步，避免纯粹为取得同一份状态再发生一次 AI 往返；没有必要把每轮内部检查的全部状态都传给 AI。

各选项主要影响或代价：A 增加等待回执长度，但可省去后续重复取状态及单独对比；B 返回较短，对只需等待结论的任务开销较小，但继续界面操作时可能需要补取。实际净收益仍需按任务验证，不承诺所有等待都省一次调用。

后续边界：完整状态指最后一次实际采集范围和能力内、无需历史拼接的信息，不伪造未采到的元素或把旧状态说成返回瞬间的新采集。超时及采集失败仍须明确，与状态一起交付时不能伪装等待成功。变化由工具内部确定性计算；基线选择及内部轮询与外部交付的衔接仍须细化，没有可比基线不能误报无变化。不改变显式取图门槛，不新增模型裁判、不自动执行下一步，不扩大为每个动作后强制获取快照。

用户原始回答：

> A

用户选择：A。

采用的答案和结论：WaitFor 结束后复用最后一次实际采集，同次返回等待结果、完整界面状态与工具内部计算的变化；不额外采集、不自动截图，采集失败或不完整如实标明。完整有效结论见 C-016。

采用原因或证据：用户明确接受 Q-015 的 A 及其前提。已采集的信息直接用于下一步，目标是减少重复取同一状态及另行对比的 AI 往返，而不是只缩短回执；未验证实际每次等待是否都省下一次调用或总 token。

结论适用前提：返回的是末次实际采集及其真实状态，不回传所有内部轮询记录、不伪造缺失内容或采集时点；等待满足、超时及采集失败仍明确区分。变化比较基线、轮询与交付衔接、会话隔离和异常协议继续细化，无可比基线不报无变化。权限、必要验证及显式取图门槛不变，不自动执行下一步。

重新裁决触发条件：末次采集不可靠、变化不一致、错误被隐藏、需要额外采集或自动截图，或实际增加补查、误操作、重试和总成本时，按具体场景重新裁决。

领域文档关联：本题确认可逆的状态交付行为，未选定新的领域术语或难以逆转的存储、协议架构；本轮仅更新原需求草稿，未修改代码。

记录状态：当前有效。

### Q-016

所属阶段: 1

关联议题：T-01、T-09 的 UI 树采集元素数量上限；关联 T-14，遵守 C-001、C-002、C-004、C-007 和 C-016。

当前事实：本轮完整读取 `tree/budget.py`，blob `5847b22190f276ef70866f82e2c95daf1481b335`，默认元素上限为 500，支持 `WINDOWS_MCP_MAX_TREE_ELEMENTS` 配置；达到预算时记录截断。该上限不是 token 数，也不限制单个节点值的文本长度。本题仅裁决是否为省 token 调低默认元素上限，不把保留逐词能力自动等同于批准任意元素数量。

完整问题：**第十六项：要不要为了省 token，调低 UI 树的默认元素数量上限？**

选项：

- **A. 不调低（推荐）**：沿用现有上限和用户配置，不为了缩短返回而更早截断；保留必要保护和截断提示。
- **B. 调低上限**：单次返回更少元素，但可能漏掉目标，让 AI 再查一次。

推荐依据及适用前提：推荐 A，前提是现有上限能满足常用任务和容量要求；避免因过早截断增加补查与判断。保留上限不代表保证完整，达到上限时仍须明确标记不完整；发现现有上限本身影响任务时另行评估，不能视为永远固定 500。

各选项主要影响或代价：A 不取得更低默认元素数带来的潜在减量，但避免人为进一步收窄采集；B 可能减小回包及采集量，却会增加漏掉目标、补查、重试及 WaitFor 条件检测受限的风险。实际净影响以任务验证为准，原建议的 150 不是已选值。

后续边界：不取消必要容量保护，不改变用户已有配置，不自动截图，不重开已经确认的逐词定位和完整状态加变化取舍。长名称、长值的文本预算、区域筛选顺序、超限恢复和具体默认值仍按后续相关问题处理，不以本题预先批准无限采集或新的自动调参机制。

用户原始回答：

> A

用户选择：A。

采用的答案和结论：不为省 token 调低 UI 树默认元素上限，沿用现有上限和用户配置，保留必要保护与截断提示。完整有效结论见 C-017。

采用原因或证据：用户明确接受 Q-016 的 A，优先避免更早截断导致目标遗漏、补查和判断；不将保留现有上限当作已证明完整或成本最优。

结论适用前提：现有上限满足常用任务和容量要求；触及上限仍标记不完整，不覆盖用户设置，不取消保护或默认自动调参。逐词能力和状态交付不变，长文本及超限恢复另行细化。

重新裁决触发条件：现有上限影响目标覆盖、条件检测、容量或性能，或用户改变配置要求时，按具体场景重新裁决。

领域文档关联：保留现有默认策略的可逆取舍，无需新建术语或 ADR；本轮只维护原需求草稿，未修改代码。

记录状态：当前有效。

### Q-017

所属阶段: 1

关联议题：T-01 的单个控件长文本预算；关联 T-04、T-14，遵守 C-001、C-002、C-004、C-005、C-007、C-016 和 C-017。

当前事实及评估边界：本轮重读 `tree/views.py` 第 20–62 行，blob `8a49958f8a450ebfcab72ce39a36c4d6376b8927`，`_node_meta_str` 直接格式化已采集的 value；既有名称格式化证据及 `tree/budget.py` 表明，限制节点数不能限制单个节点的文字长度。本题只讨论是否为省 token 把已采集的长名称或内容默认截短为预览，不重新裁决逐词定位、元素上限或文件输出的默认分页。

完整问题：**第十七项：单个控件的名称或内容很长时，要不要默认截短为预览？**

选项：

- **A. 不默认截短（推荐）**：容量允许时返回已采集的完整文本；超出必要上限时明确标记不完整。
- **B. 默认只给预览**：缩短单次返回，需要完整内容时再补读，可能增加调用和判断。

推荐依据及适用前提：推荐 A，前提是结果能在容量限制内正常交付；避免把已采到的信息人为拆成多次读取。不取消必要保护，也不要求额外采集原本没有取得的内容。

各选项主要影响或代价：A 保留较长文本的读取成本，但无需仅为恢复被省略文本再调用工具；B 可降低单次文本量，但可能遮住用于区分同名控件、理解状态或决定操作的内容，需要评估补读和判断的净影响。选 B 将影响 C-007 的不删操作信息约束，应明确调整边界，不能静默视为无损排版精简；当前尚未形成该调整。

后续边界：不以本题批准删除逐词坐标、用词节点强迫 AI 重拼正文、额外摘要模型或自动截图。原本未采到的内容不伪装成全文；具体容量阈值、超限交付和重复表达的无损整理仍在后续闭环中处理。

用户原始回答：

> A

用户选择：A。

采用的答案和结论：容量允许时返回已采集的完整控件文本，不默认截短为预览；超出必要上限时明确标记不完整。完整有效结论见 C-018。

采用原因或证据：用户明确接受 Q-017 的 A，不把已取得的信息人为拆成多次读取；接受较长正文的成本，不将这一选择当作已测量的净节省。

结论适用前提：正常容量内交付，保留必要保护、逐词坐标和操作信息；不额外采集、不以词节点强迫重拼正文、不加摘要模型或自动截图。具体阈值及超限交付仍待闭环。

重新裁决触发条件：容量、任务准确性或总体成本与预期冲突，或实施增加补读、重拼和判断时，按场景重新裁决。

领域文档关联：可逆的输出行为取舍，无新增领域术语或难以逆转的架构决定；本轮仅更新原需求草稿，未修改代码。

记录状态：当前有效。

### Q-018

所属阶段: 1

关联议题：T-11 的已有文件读取和目录筛选能力；关联 T-04、T-14、T-16，遵守 C-002、C-005 和 C-010。

当前事实及评估边界：本轮完整读取 `tools/filesystem.py`，blob `e98ba7fa45e9774d7832d61b39ae015d1f145983`。read 支持行 offset/limit，list/search 使用文件名 pattern，后两种模式没有使用 limit。本题评估已知任务范围时的使用优先级，不新增默认分页、内容语义筛选模型或自动猜测行号；文件名筛选不等同于正文内容检索。

完整问题：**第十八项：已知只需某段文件或某类文件时，要不要直接按范围筛选？**

选项：

- **A. 优先按已知范围读取（推荐）**：同一次调用指定行范围或文件名条件，取得所需结果；范围不明确时不猜、不拆成多次试读。
- **B. 优先扩大读取**：先读全文或更多目录项，再由 AI 筛选；保留更多上下文，但可能多读无关内容。

推荐依据及适用前提：推荐 A，前提是范围已有可靠依据、足以覆盖任务所需信息，且不会增加定位或补读调用。筛选已知范围，不是把所需结果强制分页；范围内结果在容量允许时仍一次完整交付。

各选项主要影响或代价：A 减少已确定无关的文本和目录项，代价是范围过窄或失效可能丢失上下文，故不能为节省强猜范围；B 可保留额外上下文，但增加主模型阅读和筛选内容。两者均尊重用户明确的范围要求和权限，不把扩大读取解释为跨越授权目录，不取消必要容量保护。实际净收益仍需比较补读、判断、准确性和全过程 token。

后续边界：只评估已有参数的使用，不把 list/search 的 limit 支持、分页游标或新全文检索能力提前当作已批准。未知范围不默认改成小批量试探；超限提示与完整内容取回仍与 T-04 一起闭环。该题不重开已确认的不强制分页和长控件文本不默认预览。

用户原始回答：

> A

用户选择：A。

采用的答案和结论：已有可靠、足够的行范围或文件名条件时，优先同次筛选读取；范围不明确时不猜、不拆成多次试读，不增加定位或补读。完整有效结论见 C-019。

采用原因或证据：用户明确选择 Q-018 的 A；使用已有范围减少无关内容，而不是把任务所需结果人为分页。实际 token 和调用收益尚未测量。

结论适用前提：范围足以覆盖任务需要且仍有效；所需范围内容量允许时一次完整交付，权限、明确范围和必要容量保护不变。不新增正文检索、分页游标或模型裁判。

重新裁决触发条件：范围失效或过窄、上下文遗漏、参数语义不符，或实际增加定位、补读、误判和重试时，按具体场景重新裁决。

领域文档关联：已有文件能力的使用取舍，无新增领域术语或难以逆转的架构决定；本轮仅更新原需求草稿，未修改代码。

记录状态：当前有效。

### Q-019

所属阶段: 1

关联议题：T-12 的图片文件压缩；关联 T-14、T-15，遵守 C-002、C-011、C-012 和 C-013。

当前事实及评估边界：本轮完整读取 `tools/_snapshot_helpers.py`，blob `8fb77fad5caf6fcbf4599f33f6b96ed7a5de6791`，当前将截图编码为 PNG，并以 Image 图片内容返回。这里讨论单纯压缩 PNG 或更换图片编码，不改变已确认的截图触发、范围及分辨率策略。尚无本次任务的压缩前后计费对照，也没有用户确认的图片传输瓶颈；不能把文件字节减量直接作为已验证的模型 token 节省。

完整问题：**第十九项：只压缩截图文件大小的优化，本轮要不要做？**

选项：

- **A. 暂不纳入（推荐）**：保留现有图片编码，优先处理有明确 token 优化路径的项目。
- **B. 一并纳入**：同时优化图片传输和存储，接受额外开发及兼容性验证工作。

推荐依据及适用前提：推荐 A，前提是尚无已确认的传输瓶颈或实测 token 收益；本轮先聚焦减少模型输入和 AI 往返，不把文件变小直接计作 token 已节省。若用户实际需要兼顾传输、存储或容量问题，B 是独立且合理的范围扩展，不把该收益混入未测量的 token 收益。

各选项主要影响或代价：A 不取得额外文件压缩的潜在传输收益，但不新增编码策略和验证负担；B 增加传输性能优化范围，需要验证编码耗时、大小、客户端兼容、文字细节可读性及重试，不能通过降低已确定的分辨率或自动多次传图实现。具体编码及参数未选定，不默认采用有损压缩，也不把 PNG 压缩和 JPEG 转换混作同一效果。

后续边界：本题仅裁决范围，不授权实施代码或改变显式截图门槛。没有计费证据时，文件字节、传输耗时和模型 token 分别记录；T-13 的内部重复渲染仍另行评估，不由本题一并通过或排除。

用户原始回答：

> A

用户选择：A。

采用的答案和结论：单纯压缩截图文件或更换图片编码暂不纳入本轮，保留现有编码，优先处理有明确 token 优化路径的项目。完整有效结论见 C-020。

采用原因或证据：用户明确选择 Q-019 的 A；在尚无已确认传输瓶颈或实测 token 收益时不扩大范围，不把文件变小当作模型输入已经减少。

结论适用前提：现有截图触发、范围、分辨率和必要容量保护不变；不禁止今后单独处理传输或存储问题，不由本题排除 T-13 的内部计算清理。

重新裁决触发条件：确认传输、存储或容量瓶颈，获得实际 token 或重试收益证据，或用户明确扩展范围时重新裁决。

领域文档关联：本题为可逆的工作范围取舍，无新增领域术语或难以逆转的架构决定；本轮只维护原需求草稿，未修改代码。

记录状态：当前有效。

### Q-020

所属阶段: 1

关联议题：T-13 的内部重复渲染；关联 T-14、T-15，遵守 C-002、C-007 和 C-020。

当前事实及评估边界：上轮完整读取的 `tools/_snapshot_helpers.py`，blob `8fb77fad5caf6fcbf4599f33f6b96ed7a5de6791`，在 capture_desktop_state 中生成 interactive、scrollable、semantic 三种文本，build_snapshot_response 实际仅把 semantic tree 追加到 UI Tree。前两种文本仍在内部进行清理和修复，但没有加入该快照响应。本题只评估这些未返回文本的生成和处理清理，不删除 AI 已收到的操作信息，不将三份内部字符串误写为三份模型输入；尚无证据表明此处计算导致了实际超时或重试。

完整问题：**第二十项：只减少程序内部计算、不减少 AI 接收内容的优化，本轮要不要做？**

选项：

- **A. 暂不纳入（推荐）**：先聚焦 token、工具调用和 AI 判断，内部性能清理另行处理。
- **B. 一并纳入**：顺手删除未返回给 AI 的重复文本生成，接受额外回归验证。

推荐依据及适用前提：推荐 A，前提是没有证据表明这部分计算造成超时或重试；不把程序提速直接算作 token 节省。此处仅判断本轮范围，不否认减少本地计算的性能价值，也不排除今后用实测证据评估其间接收益。

各选项主要影响或代价：A 不取得本项潜在耗时和内存收益，但不扩大当前优化与回归范围；B 可以减少无效内部处理，但需核对其他调用方、异常行为和输出一致性，且在模型输入和调用链不变时没有直接 token 减量。两项均不影响已确认的 UI 树排版精简、完整状态加变化以及 WaitFor 末次采集复用，不把本题扩张为禁止所有内部重构。

后续边界：尚未选择实现方案，也未授权修改代码；真正减少超时、失败重试的性能修复不能仅因属于内部计算就被一概排除。具体验收口径、快照范围与基线、容量保护和故障恢复等其他未决项继续保留。

用户原始回答：

> A

用户选择：A。

采用的答案和结论：未返回文本的内部性能清理暂不纳入本轮，先聚焦 token、工具调用和 AI 判断；不把程序提速直接计作 token 节省。完整有效结论见 C-021。

采用原因或证据：用户明确选择 Q-020 的 A，接受在未证明这部分计算导致超时或重试时不扩大范围。

结论适用前提：不影响已选的排版精简、完整状态加变化和等待末次采集复用，不禁止必要内部重构或有证据减少超时、重试的修复。

重新裁决触发条件：确认该部分造成性能瓶颈、超时或重试，取得间接 token 收益证据，或用户明确扩展范围时重新裁决。

领域文档关联：可逆的范围取舍，无新增领域术语或难以逆转的架构决定；本轮仅更新原需求草稿，未修改代码。

记录状态：当前有效。

### Q-021

所属阶段: 1

关联议题：T-03、T-09 的文字 UI 树观察范围；关联 T-14、T-16，遵守 C-001、C-002、C-004、C-007、C-011、C-016、C-017 和 C-018。

评估边界：Q-011 只裁决截图范围，未自动确定文字 UI 树范围。本题针对已知可靠区域的观察策略，不假定已经实现新的窗口选择器、自动范围识别或前置筛选机制，不改变现有逐词能力和长文本交付；范围内完整状态仍受实际采集能力和必要容量保护约束。

完整问题：**第二十一项：获取文字 UI 树时，目标区域已知，要不要优先只观察该区域？**

选项：

- **A. 优先限定已知范围（推荐）**：同次调用返回范围内的完整状态＋变化；范围未知或需要全局信息时不缩小范围。
- **B. 不主动限定范围**：保留更多周边信息，但 AI 也要读取更多无关内容。

推荐依据及适用前提：推荐 A，前提是区域可靠、足以覆盖任务需要，且不为缩小范围增加定位或补查调用。只利用已有信息，不引入额外模型来判断范围；不强迫先观察整个桌面才能使用已知范围。

各选项主要影响或代价：A 可减少任务范围外的无关 UI 信息，但范围过窄、失效或遗漏弹窗会损害后续判断，因此必须满足覆盖前提；B 更保守地保留周边上下文，代价是主模型读取和筛选更多文本。两项均尊重用户显式指定的范围及必要安全信息，不以范围优化替代权限、确认或结果验证；实际净收益须比较覆盖、补查、重试和全过程 token。

后续边界：不自动附图，不缩短范围内控件文本、不删除逐词坐标、不调低元素上限，不退回仅差分返回。范围变化的基线可比性、筛选与预算顺序、必要全局元数据及故障提示仍需闭环；区域外未观察到的内容不能被报告为不存在或已删除。WaitFor 仍复用末次实际采集，不为满足本题增加另一次采集。本题不构成实现授权。

用户原始回答：

> A

用户选择：A。

采用的答案和结论：文字 UI 树优先限定已有可靠、足够的目标区域，同次返回范围内完整状态与变化；未知范围或需全局信息时不缩小，不增加定位或补查。完整有效结论见 C-022。

采用原因或证据：用户明确选择 Q-021 的 A，独立确认文字观察的范围策略；不将此前截图裁剪结论自动扩展为本题答案，也不把预期减量写成实测收益。

结论适用前提：区域有效且覆盖必要信息，保留权限、必要确认、验证及安全信息；范围内逐词坐标和完整文本不删减，元素上限不调低，不自动附图。区域外未观察到不等于不存在或删除，WaitFor 不额外采集。范围比较基线、预算顺序与故障交付继续闭环。

重新裁决触发条件：范围失效、遗漏弹窗或必要上下文、比较误报、采集信息不足，或实际增加定位、补查、误操作、重试与总体成本时，按具体场景重新裁决。

领域文档关联：本题为可逆的观察范围策略，无新增领域术语或难以逆转的架构决定；本轮仅维护原需求草稿，未修改代码。

记录状态：当前有效。

### Q-022

所属阶段: 1

关联议题：T-14 的效果验收范围；关联 T-15、T-16，遵守 C-002 及各已确认行为约束。

评估边界：现有结论已明确应关注全过程成本，而非只看单次返回长度；尚无用户实际任务日志、客户端用量或 Windows 对照结果。本题不重复裁决优化目标，而是确定完整任务的优化前后实测对照是否作为本轮验收要求。既有快照 profile 记录耗时，不能代替完整模型用量。

完整问题：**第二十二项：验收时，要不要实际对比完成同一任务的总消耗？**

选项：

- **A. 纳入实测对照（推荐）**：比较优化前后的总 token、工具调用、AI 判断轮次和重试，任务成功率不能下降；需要安排对照测试。
- **B. 本轮只验收功能规则**：检查已选行为是否实现，暂不安排总消耗实测；实际节省仍标记为未验证。

提问时的推荐依据及适用前提：推荐 A，前提是用相同客户端、模型和任务条件对照；避免单次返回变短，整个任务反而更贵。任务成功的定义和必要验证在两组中保持一致，不能以少做工作、提前失败或降低完成标准换取较低 token。此为原推荐；用户随后将实测范围改为修改点局部对照，不继续把完整任务实测作为已确认要求。

各选项主要影响或代价：A 需要准备可重复任务、收集实际用量及执行记录，能核对已选方案的净效果；B 减少本轮实测准备，但只能证明行为规则实现，不能据此宣称总 token 已降低或给出节省比例。两项都保留 C-002 的全过程成本目标及已有安全、能力和信息完整性约束，B 仅暂缓实测，不把单次文本长度当作充分证据。

提问时的后续边界：具体客户端、模型、代表性任务、样本规模、允许波动和目标幅度尚未选定，不编造百分比。总用量须涵盖实际发生的主模型、额外 sampling、图像和重试；缓存命中及计费与未缓存 token 分开记录，避免漏算或重复计数。AI 判断轮次须采用可观测的交互日志口径，不声称可数出模型隐藏推理中的每次判断，工具内部轮询也不算新增 AI 往返。无法取得的用量如实标记缺失，不用字符数或耗时冒充实测 token。不新增每次生产调用都要运行的模型评判器或把整份测试日志自动塞进普通工具响应，本题不授权立即运行有副作用的真实业务测试。用户本轮修正后，实际验收按 C-023 收窄为局部、小范围测试；原完整任务候选的未决项不自动转为必需的大范围测试准备。

用户原始回答：

> 对修改的内容做修改前后token消耗的"局部"测试,把范围局限在"小"范围内,尽量派出模型智力的因素

用户选择：自定义修正。保留 token 前后测试，但只做修改点局部、小范围对照，尽量排除模型智力因素；不是原 A 的完整自主任务实测，也不是原 B 的只验功能。

采用的答案和结论：采用 C-023。固定输入、样本和初始状态，直接比较改动点处理前后的 token；涉及调用合并或状态复用时，只比较同一局部结果对应的最小固定操作链。分开报告 token 和调用增减，保持功能及信息完整性，不让模型自由规划或临场决策成为主要变量。

采用原因或证据：用户明确要求“局部”“小”范围，并尽量排除模型智力的影响。原问题把验收分成完整任务实测或不测 token 两类，遗漏了受控局部测试这一组合，现按用户原意修正。原文中的“派出”按上下文解释为“排除”，原始引文不改写。未执行测试，不虚构数值或节省率。

结论适用前提：小范围样本及执行路径可固定；用一致且明确的计数口径，必要验证和信息不减少。涉及实际生成或图像的非确定部分不伪装成纯文本实测；固定回放和估算须标明。局部计数不等于实际账单或端到端成功率，不能推算每次隐藏判断的 token，更不能把局部差额冒充全任务净收益。具体计数器、样本规模与阈值仍待细化。

重新裁决触发条件：局部不能隔离效果、波动主导结果、计量口径不一致、漏掉必要输出或验证，或必须扩大测试范围才能判断某项影响时，先说明局限再裁决，不自行恢复完整自主任务测试。

领域文档关联：本题确认测试范围和方法约束，不是新的项目领域术语或难以逆转的架构决定；本轮只更新原需求草稿，未修改代码或执行测试。

记录状态：当前有效；原 A 的完整任务实测推荐已由本自定义结论取代，原题及选项保留用于追溯。

### Q-023

所属阶段: 1

关联议题：T-03、T-10 的变化比较基线，关联 T-16，遵守 C-004、C-011、C-016、C-022 和 C-023。

评估边界：已经确认完整状态加变化同次返回，并在 WaitFor 中复用末次采集；尚未确定变化相对哪一次观察。本题只确定默认比较对象，不重问是否返回完整状态、不决定持久化存储或新增模型判断。可用固定的前后状态和内部轮询序列做局部验证，不要求自主模型任务。

完整问题：**第二十三项：快照里的“变化内容”，默认和哪份状态比较？**

选项：

- **A. 上次返回给 AI 的可比状态（推荐）**：限同一会话；WaitFor 内部轮询不替换比较基线，避免漏掉 AI 尚未见过的变化。
- **B. 最近一次内部采集的可比状态**：只显示相邻采集之间的变化，可能遗漏两次对外交付之间较早发生的变化。

推荐依据及适用前提：推荐 A，前提是同一会话存在有效、范围可比的已返回状态；让变化对应 AI 两次观察之间的差异，不增加工具调用。首次或没有可比状态时明确说明不能比较，不能当作无变化。A 保留的比较间隔较长，变化列表可能比 B 更长，接受这一代价以减少自行对照。

各选项主要影响或代价：A 可覆盖两次对外交付的净变化，但需独立维护交付基线；B 可反映最近内部检查间的细小变化，但不等于 AI 从上次收到状态以来的完整差异，可能需要另行比较。两项都只返回本次完整状态加变化，不回传全部内部轮询记录，也不自动截图或额外采集。

后续边界：不跨会话混用，不把未观察区域或截断导致的缺项当作已删除。连接重建、交付失败、范围变化、部分采集和标签有效性的具体处理仍需闭环；本题未选定会话识别协议、存储期限或基线版本参数。变化仅表示两个有效状态间的差异，不承诺恢复中间出现后又消失的全部瞬态事件。

#### 同题澄清：Q-023-A

用户澄清原文：

> "上次"怎么定义？先用原版跑一遍吗?

已解释的区分：运行时“上次”指同一会话中最近一次已成功返回给 AI 的有效可比状态，而非工具内部轮询；它需要保存交付基线。先跑原版、再跑修改版属于 C-023 的局部前后测试，不是正常使用时的变化基线。第一次使用可以正常返回完整状态并说明无基线，不要求先运行原版。

澄清时实际展示的问题：**变化基线定义：**

- **A. 最近一次成功返回给 AI 的可比状态（推荐）**
- **B. 最近一次工具内部采集状态**
- **C. 不保存基线，每次只返回当前完整状态+不计算变化**

推荐依据和代价：A 对应 AI 两次观察之间的差异，代价是保存交付基线；B 更接近内部相邻采集，但可能遗漏 AI 未见过的变化；C 会放弃已确认的变化计算，需改变 C-004，用户未选 C。此澄清仍属于 Q-023，不独立增加问题序号。

用户原始回答：

> A

用户选择：Q-023 同题澄清 Q-023-A 的 A。

采用的答案和结论：最近一次成功返回给 AI 的可比状态作为运行时变化基线；WaitFor 内部轮询不更新，失败采集与未交付状态不更新。首次无基线、观察范围不可比或窗口对象失效时不强行 diff，明确说明；原版/修改版仅用于局部测试。完整有效结论见 C-024。

采用原因或证据：用户在运行时基线与测试基线分开解释后明确选择 A。先前只在聊天中记录，本轮核对仓库仍待回答后补记，不重复让用户确认。

结论适用前提：同一会话、已交付、有效且可比；不跨会话、不伪造删除、不新增 diff 或截图，不把响应生成成功当作已证明 AI 收到。具体交付成功判据和异常时序待闭环，不预先批准额外 AI 确认调用。

重新裁决触发条件：交付基线不可靠、会话或范围混用、内部轮询覆盖，或增加确认与补查时，按具体问题重新裁决。

领域文档关联：`CONTEXT.md` 的“交付基线”；不新增 ADR，未修改代码。

记录状态：当前有效。

### Q-024

所属阶段: 1

关联议题：T-03、T-10 的交付基线保留范围；遵守 C-004、C-016 和 C-024。

完整问题：**第二十四项：变化内容需要保留多久？**

选项（保留已展示含义；本题实际裁决基线保留数量和维护责任，不是时间长度）：

- **A. 只保留最近一次 AI 已收到的状态作为基线（推荐）**：最简单；不需要历史链；满足“上次看到有什么变化”。
- **B. 保留多个历史状态**：可以支持回溯；但增加存储、管理和判断复杂度。
- **C. 不由 Windows-MCP 保存，由 AI 自己维护历史**：工具更简单；但增加 AI 上下文负担和判断成本。

推荐依据及适用前提：推荐 A，适用于只需对照最近一次已交付状态的运行时变化，符合减少 AI 判断和工具调用的目标，不引入历史管理；不声称已经测量出某项存储节省。

各选项主要影响或代价：A 不提供多版本回溯，比较时仍要验证可比性；B 扩大历史保留和管理范围；C 把状态保存和传回的责任转移给 AI，可能增加上下文和调用，不符合当前优先级。

用户原始回答：

> A

用户选择：A。

采用的答案和结论：只保留同一会话最近一次成功返回给 AI 的状态作为候选交付基线，不维护多版本历史链，不让 AI 自行保存、拼接或传回历史；完整有效结论见 C-025。

采用原因或证据：用户接受 A；这是此前已经在聊天中选择、此次补齐的记录，不重新编号或重复询问。

结论适用前提：沿用 C-024；基线只用于后续变化比较，内部轮询、失败采集和未交付状态不更新。无基线或不可比明确说明；不由本题决定跨重启保存、磁盘持久化或过期时间。

重新裁决触发条件：必须支持多版本或多范围对照、单基线导致反复不可比与补查，或会话生命周期不能满足需求时再裁决。

领域文档关联：`CONTEXT.md` 的“交付基线”；本题没有选定难以逆转的存储架构，不新增 ADR，未修改代码。

记录状态：当前有效。

### Q-025

所属阶段: 1

关联议题：T-03 的变化内容粒度；关联 T-06、T-14、T-16，遵守 C-004、C-007、C-016、C-023 至 C-025。

完整问题：**第二十五项：变化内容的粒度要到什么程度？**

选项：

- **A. 只描述高层变化（推荐）**：例如窗口变化、元素新增/删除、文本变化；token 较少，适合大多数判断。
- **B. 精确到元素字段变化**：例如某控件的 name、value、enabled、checked、坐标分别变化；信息更完整，但变化内容可能较长。
- **C. 完整状态 + 变化路径索引**：返回完整状态，同时变化只提供节点 ID/路径引用；最省重复信息，但依赖稳定节点标识。

提问时的推荐依据及适用前提：推荐 A，先避免生成大量变化细节；若后续确需精确字段，可基于同次已提供的完整当前状态定位，不增加默认输出。此推荐的“较少”“最省”和适合大多数判断是未经测量的预期，不作为已验证收益。当前状态无法单独说明被省去的历史差异，因此这项推荐不授权把必要历史比较转回 AI；遇到此类缺口须按重新裁决条件处理。

各选项主要影响或代价：A 较少重复字段级信息，但摘要过粗会影响对象定位与变化理解；B 提供更细前后差异，代价是更长的变化输出；C 减少重复正文但要求解析引用和稳定标识。各项都不能取消完整当前状态、伪报变化或自动截图，实际文本量及正确性按局部测试判断。

用户原始回答：

> A

用户选择：A。

采用的答案和结论：变化内容采用工具内部计算的高层摘要，保留完整当前状态，不默认逐字段展开或只给路径索引。完整有效结论见 C-026。

采用原因或证据：用户本轮明确选择 A，接受降低变化附加内容粒度的取舍；并未要求删减完整状态、由额外模型写摘要或增加精确差异补查。

结论适用前提：摘要足以清楚说明受影响对象及高层变化类别；不丢完整状态中的操作信息，不把未观察、截断或不可比较当作删除或无变化。必要比较由工具承担，局部测试不依赖模型自由推理。

重新裁决触发条件：摘要不足以说明重要变化、对象含混，导致 AI 重新比较历史、补查或误判时，提交具体缺口重新裁决，不静默升级为逐字段长输出或压缩为模糊提示。

领域文档关联：`CONTEXT.md` 的“变化摘要”；可逆粒度取舍，未修改代码，不新增 ADR。

记录状态：当前有效。

### Q-026

所属阶段: 1

关联议题：T-05 的显式额外 AI 摘要能力；关联 T-07、T-14、T-15，遵守 C-002、C-006、C-009 和 C-023。

评估边界：Q-005 已决定默认不做额外 AI 摘要，但没有决定删除显式请求能力。既有 `tools/scrape.py` blob `ad11580fc8f8279cc47475a33274e9cc61aa8175` 存在 use_sampling 参数和 sampling 路径；本轮仓库 src 子树与原证据一致。本题只裁决显式能力是否保留，不重新询问默认策略，不宣称客户端一定支持 sampling。

完整问题：**第二十六项：Scrape 默认不做额外 AI 摘要后，要不要保留“显式开启”的能力？**

选项：

- **A. 保留显式开启（推荐）**：默认关闭，仅明确请求时才尝试额外模型摘要；保留可选能力。
- **B. 彻底移除**：抓取工具不再调用额外模型；需要调整或移除相关旧参数及用法。

推荐依据及适用前提：推荐 A，前提是不增加默认调用或每次必做的模式判断；不为关闭默认开销而一并删除可选能力。默认直接抓取，明确需要摘要时可在原请求表达，不强迫先调用一次纯抓取再开启。

各选项主要影响或代价：A 保留显式路径及其维护、兼容验证，只有明确启用时承担额外模型开销；B 简化内部模型路径，但取消已有可选行为，需明确旧参数和 query 的迁移，不得静默忽略。两项均保留程序清理、必要容量保护和不完整提示；不会恢复默认摘要。

后续边界：query 是否构成显式请求、与 use_sampling 的组合、客户端不支持或调用失败的回退仍需闭环；本题不默认为自然语言 query 自动开启模型，也不引入额外模型判断是否需要摘要。按 C-023 用局部固定样本检查路径和计量，不用自由摘要结果虚构节省值。尚未授权实施代码。

用户原始回答：待回答。

用户选择：待回答。

采用的答案和结论：尚未形成。

结论适用前提与重新裁决触发条件：待收到回答后记录。

记录状态：待回答。

## Grill-Me 工作流状态

- WORKFLOW_ID: `GRILL_ME_GENERIC`
- revision: `v1-directory-orchestrator`
- 当前阶段: `1`
- 各阶段状态:
  - 阶段 1: `WAITING_USER`
  - 阶段 2: `NOT_STARTED`
  - 阶段 3: `NOT_STARTED`
  - 阶段 4: `NOT_STARTED`
  - 阶段 5: `NOT_STARTED`
  - 阶段 6: `NOT_STARTED`
  - 阶段 7: `NOT_STARTED`
  - 阶段 8: `NOT_STARTED`
- 进入模式: `FIRST`
- 待回答: `Q-026`
- 恢复起点: 无
- 恢复上限: 无
- 阻碍: 无
- 最后更新时间: `2026-09-15（Asia/Tokyo），Q-023 同题澄清、Q-024、Q-025 已记录，Q-026 待回答`
